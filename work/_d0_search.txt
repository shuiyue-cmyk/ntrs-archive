==== C:/Users/zouyu/Downloads/酒馆/数据库/数据库8.7.2.js size 7932756
[$0] "======\\n\\n以下是当前的<当前表格数据>,记录有本轮之前的数据，你的一切操作指令都必须在这个<当前表格数据>的基础与指导上进行：\\n<当前表格数据>\\n$0\\n</当前表格数据>\\n\\n$8\",\n            \"deletable\": false,\n            \"mainSlot\": \"B\",\n            \"isMa"
[$0] " default: _shim_fs,\n        readFileSync: readFileSync\n    });\n\n    var require$$0 = /*@__PURE__*/getAugmentedNamespace(_shim_fs$1);\n\n    var _shim_crypto = {}; const randomFillSyn"
[$0] "peof __filename?ya=__filename:ba&&(ya=self.location.href);if(ca){var fs=require$$0;1<process.argv.length&&(wa=process.argv[1].replace(/\\\\/g,\"/\"));process.argv.slice(2);\"undefined\"!"
[$0] " if (_seedRowsTablesUsed_ACU.length > 0) {\n            logDebug_ACU(`[SeedRows] $0 使用 seedRows 作为基础数据：${_seedRowsTablesUsed_ACU.join('、')}`);\n        }\n        let messagesText = '"
[当前表格数据] "格数据执行增删改操作。\\n\\n## 核心任务\\n依据三类资料来源执行表格编辑：\\n- <背景设定>：故事及人物设定\\n- <正文数据>：上轮发生的故事\\n- <当前表格数据>：之前的数据作为填表基础\\n\\n## 输出格式（严格执行）\\n\\n<thought>\\n[分析当前轮次的剧情变化]\\n[阅读所有填表相关规则]\\n[根据填表规则确定需要修改的表格和字段]"
[当前表格数据] "前轮次的剧情变化]\\n[阅读所有填表相关规则]\\n[根据填表规则确定需要修改的表格和字段]\\n[逐步推理每个修改操作，说明理由]\\n针对纪要表的额外规则：如果<当前表格数据>里存在纪要表，那么本轮就必须对其进行插入一条新的总结记录。\\n日志与纪要语气校准：你在思考纪要时，必须区分“正常恋爱互动”与“暗黑主从文风”。你可以使用正常的交流词汇（如：提议、要求、同"
[当前表格数据] "\n            \"content\": \"现在请按照我的要求立刻开始你的工作 \\n========================\\n\\n以下是当前的<当前表格数据>,记录有本轮之前的数据，你的一切操作指令都必须在这个<当前表格数据>的基础与指导上进行：\\n<当前表格数据>\\n$0\\n</当前表格数据>\\n\\n$8\",\n            \"de"
[当前表格数据] "立刻开始你的工作 \\n========================\\n\\n以下是当前的<当前表格数据>,记录有本轮之前的数据，你的一切操作指令都必须在这个<当前表格数据>的基础与指导上进行：\\n<当前表格数据>\\n$0\\n</当前表格数据>\\n\\n$8\",\n            \"deletable\": false,\n            \"main"
[contextExtractRules] " 25,\n        \"extractTags\": \"recall\",\n        \"extractInjectTags\": \"\",\n        \"contextExtractRules\": [],\n        \"contextExcludeRules\": [\n            {\n                \"start\": \
[contextExtractRules] "t\",\n        \"extractInjectTags\": \"\",\n        \"contextExtractTags\": \"\",\n        \"contextExtractRules\": [],\n        \"contextExcludeTags\": \"\",\n        \"contextExcludeRules\": [],\n 
[contextExtractRules] "ules_ACU() {\n        return normalizeExtractRules_ACU(DEFAULT_PLOT_SETTINGS_ACU.contextExtractRules, DEFAULT_PLOT_SETTINGS_ACU.contextExtractTags || \"\");\n    }\n    function getDefa"
[contextExtractRules] "();\n                const extractRules = normalizeExtractRules_ACU(plotSettings.contextExtractRules, extractTags);\n                const excludeTags = (plotSettings.contextExcludeT"
[tableData] "shot 行已经在临时 SQLite 中实际执行。\n     */\n    async function hydrateTableDataStrict_ACU(tableData) {\n        const engine = new SqliteEngine();\n        const syncBridge = new SyncBridge(en"
[tableData] "try {\n            await engine.init();\n            syncBridge.loadFromTableData(tableData, { strict: true });\n        }\n        finally {\n            engine.dispose();\n        }\n  "
[tableData] "pose();\n        }\n    }\n    async function validateSqliteTemplateDataStrict_ACU(tableData) {\n        try {\n            await hydrateTableDataStrict_ACU(tableData);\n            retu"
[tableData] "ict_ACU(tableData) {\n        try {\n            await hydrateTableDataStrict_ACU(tableData);\n            return { success: true };\n        }\n        catch (error) {\n            retu"
[sheetData] "eginning with \"sheet_\".' };\n        }\n        if (!isObjectRecord_ACU$1(options.sheetData)) {\n            return { error: `V2 sheet checkpoint requires object sheetData for ${optio"
[sheetData] ".sheetData)) {\n            return { error: `V2 sheet checkpoint requires object sheetData for ${options.sheetKey}.` };\n        }\n        if (!options.reason) {\n            return {"
[sheetData] "validation.error };\n        const normalizedSheetData = deepClone_ACU$1(options.sheetData);\n        const normalization = normalizeCanonicalTableRows_ACU({ [options.sheetKey]: norm"
[sheetData] "upShards_ACU(options) {\n        const requestedEntries = Object.entries(options.sheetDataByKey || {})\n            .filter(([sheetKey]) => sheetKey.startsWith('sheet_'));\n        if"
[guideData] "eedRows = getEffectiveSeedRowsForSheet_ACU(String(sheet.uid), {\n                guideData: null,\n                allowTemplateFallback: true,\n            });\n            if (Array."
[guideData] "unction commitCurrentFloorTemplateScopeOnly_ACU(options) {\n        if (!options.guideData || typeof options.guideData !== 'object' || Array.isArray(options.guideData)) {\n          "
[guideData] "mplateScopeOnly_ACU(options) {\n        if (!options.guideData || typeof options.guideData !== 'object' || Array.isArray(options.guideData)) {\n            return { saved: false, err"
[guideData] "ons.guideData || typeof options.guideData !== 'object' || Array.isArray(options.guideData)) {\n            return { saved: false, error: 'scope-only 模板提交必须提供有效的 guideData。' };\n     "
[表格数据] "     {\n            \"role\": \"USER\",\n            \"content\": \"你是【填表AI】，负责根据用户提供的资料对表格数据执行增删改操作。\\n\\n## 核心任务\\n依据三类资料来源执行表格编辑：\\n- <背景设定>：故事及人物设定\\n- <正文数据>：上轮发生的故事\\n- <当前表格数据>：之前的数据作为填表基础"
[表格数据] "据执行增删改操作。\\n\\n## 核心任务\\n依据三类资料来源执行表格编辑：\\n- <背景设定>：故事及人物设定\\n- <正文数据>：上轮发生的故事\\n- <当前表格数据>：之前的数据作为填表基础\\n\\n## 输出格式（严格执行）\\n\\n<thought>\\n[分析当前轮次的剧情变化]\\n[阅读所有填表相关规则]\\n[根据填表规则确定需要修改的表格和字段]\\n"
[表格数据] "次的剧情变化]\\n[阅读所有填表相关规则]\\n[根据填表规则确定需要修改的表格和字段]\\n[逐步推理每个修改操作，说明理由]\\n针对纪要表的额外规则：如果<当前表格数据>里存在纪要表，那么本轮就必须对其进行插入一条新的总结记录。\\n日志与纪要语气校准：你在思考纪要时，必须区分“正常恋爱互动”与“暗黑主从文风”。你可以使用正常的交流词汇（如：提议、要求、同意、"
[表格数据] "           \"content\": \"现在请按照我的要求立刻开始你的工作 \\n========================\\n\\n以下是当前的<当前表格数据>,记录有本轮之前的数据，你的一切操作指令都必须在这个<当前表格数据>的基础与指导上进行：\\n<当前表格数据>\\n$0\\n</当前表格数据>\\n\\n$8\",\n            \"dele"
[plotSettings] "tatus_full`;\n    const STORAGE_KEY_PLOT_SETTINGS_ACU = `${SCRIPT_ID_PREFIX_ACU}_plotSettings`;\n    // ═══════════════════════════════════════════════════════════════\n    // Profile"
[plotSettings] "Run) {\n        if (dryRun)\n            return false;\n        if (!settings_ACU?.plotSettings?.enabled)\n            return false;\n        if (isQuietLikeGeneration_ACU(type, params)"
[plotSettings] "   standardizedTableFillEnabled: true,\n        toastMuteEnabled: false,\n        plotSettings: JSON.parse(JSON.stringify(DEFAULT_PLOT_SETTINGS_ACU)),\n        plotPresetBindings: {},"
[plotSettings] "ontent);\n        return base;\n    }\n    function getLegacyPlotPromptContent_ACU(plotSettings, promptId) {\n        try {\n            const p = plotSettings?.prompts;\n            if "
[占位符] "ay(src) ? JSON.parse(JSON.stringify(src)) : [];\n        // 如果提供了主内容，替换 $CONTENT 占位符\n        if (mainContent) {\n            base.forEach((item) => {\n                if (item.content"
[占位符] "+参数；seedRows 仅保留字段，不默认展开到 content）\n        // - 否则返回 null，让调用方按旧逻辑处理（例如用完整模板结构作为占位符）\n        if (foundCount <= 0) {\n            if (hasSheetGuide) {\n                // 直接物化：仅表头（see"
[占位符] "L 中的中文名替换为英文名（跳过字符串值）\n         *\n         * 安全替换策略：\n         * 1. 先把单引号字符串提取出来，用占位符替代\n         * 2. 在安全的 SQL 上做中文→英文替换（长名称优先，避免子串误匹配）\n         * 3. 把字符串值放回去\n         */\n        tra"
[占位符] "{\n            if (!sql)\n                return sql;\n            // 1. 提取单引号字符串，用占位符替代\n            const strings = [];\n            let safeSql = sql.replace(/'[^']*'/g, (match) => {"
[替换] "           initNode: \"游戏初始化时，根据设定为主角添加初始技能。\",\n            deleteNode: \"技能因剧情被剥夺或替换时删除。\\nSQL示例: DELETE FROM protagonist_skills WHERE skill_name = '被剥夺的技能';\",\n            updateNode:"
[替换] "olor='#ffc107';\">${escapeHtml_ACU$1(label)}</button>`;\n    }\n    /**\n     * 生成正文替换 toast 中的\"重新优化\"按钮 HTML\n     */\n    function renderReoptButton_ACU() {\n        return `<button id=\""
[替换] "}...\"`);\n                }\n            }\n        }\n        logDebug_ACU(`[正文优化] 替换统计: 成功 ${appliedCount}/${optimizations.length}，失败 ${failedCount}`);\n        if (failedItems.length"
[替换] " Array.isArray(src) ? JSON.parse(JSON.stringify(src)) : [];\n        // 如果提供了主内容，替换 $CONTENT 占位符\n        if (mainContent) {\n            base.forEach((item) => {\n                if ("
==== C:/Users/zouyu/Downloads/酒馆/数据库/数据库8.8.1.js size 8127077
[$0] "======\\n\\n以下是当前的<当前表格数据>,记录有本轮之前的数据，你的一切操作指令都必须在这个<当前表格数据>的基础与指导上进行：\\n<当前表格数据>\\n$0\\n</当前表格数据>\\n\\n$8\",\n            \"deletable\": false,\n            \"mainSlot\": \"B\",\n            \"isMa"
[$0] " default: _shim_fs,\n        readFileSync: readFileSync\n    });\n\n    var require$$0 = /*@__PURE__*/getAugmentedNamespace(_shim_fs$1);\n\n    var _shim_crypto = {}; const randomFillSyn"
[$0] "peof __filename?ya=__filename:ba&&(ya=self.location.href);if(ca){var fs=require$$0;1<process.argv.length&&(wa=process.argv[1].replace(/\\\\/g,\"/\"));process.argv.slice(2);\"undefined\"!"
[$0] " if (_seedRowsTablesUsed_ACU.length > 0) {\n            logDebug_ACU(`[SeedRows] $0 使用 seedRows 作为基础数据：${_seedRowsTablesUsed_ACU.join('、')}`);\n        }\n        let messagesText = '"
[当前表格数据] "格数据执行增删改操作。\\n\\n## 核心任务\\n依据三类资料来源执行表格编辑：\\n- <背景设定>：故事及人物设定\\n- <正文数据>：上轮发生的故事\\n- <当前表格数据>：之前的数据作为填表基础\\n\\n## 输出格式（严格执行）\\n\\n<thought>\\n[分析当前轮次的剧情变化]\\n[阅读所有填表相关规则]\\n[根据填表规则确定需要修改的表格和字段]"
[当前表格数据] "前轮次的剧情变化]\\n[阅读所有填表相关规则]\\n[根据填表规则确定需要修改的表格和字段]\\n[逐步推理每个修改操作，说明理由]\\n针对纪要表的额外规则：如果<当前表格数据>里存在纪要表，那么本轮就必须对其进行插入一条新的总结记录。\\n日志与纪要语气校准：你在思考纪要时，必须区分“正常恋爱互动”与“暗黑主从文风”。你可以使用正常的交流词汇（如：提议、要求、同"
[当前表格数据] "\n            \"content\": \"现在请按照我的要求立刻开始你的工作 \\n========================\\n\\n以下是当前的<当前表格数据>,记录有本轮之前的数据，你的一切操作指令都必须在这个<当前表格数据>的基础与指导上进行：\\n<当前表格数据>\\n$0\\n</当前表格数据>\\n\\n$8\",\n            \"de"
[当前表格数据] "立刻开始你的工作 \\n========================\\n\\n以下是当前的<当前表格数据>,记录有本轮之前的数据，你的一切操作指令都必须在这个<当前表格数据>的基础与指导上进行：\\n<当前表格数据>\\n$0\\n</当前表格数据>\\n\\n$8\",\n            \"deletable\": false,\n            \"main"
[contextExtractRules] " 25,\n        \"extractTags\": \"recall\",\n        \"extractInjectTags\": \"\",\n        \"contextExtractRules\": [],\n        \"contextExcludeRules\": [\n            {\n                \"start\": \
[contextExtractRules] "t\",\n        \"extractInjectTags\": \"\",\n        \"contextExtractTags\": \"\",\n        \"contextExtractRules\": [],\n        \"contextExcludeTags\": \"\",\n        \"contextExcludeRules\": [],\n 
[contextExtractRules] "ules_ACU() {\n        return normalizeExtractRules_ACU(DEFAULT_PLOT_SETTINGS_ACU.contextExtractRules, DEFAULT_PLOT_SETTINGS_ACU.contextExtractTags || \"\");\n    }\n    function getDefa"
[contextExtractRules] "();\n                const extractRules = normalizeExtractRules_ACU(plotSettings.contextExtractRules, extractTags);\n                const excludeTags = (plotSettings.contextExcludeT"
[tableData] "shot 行已经在临时 SQLite 中实际执行。\n     */\n    async function hydrateTableDataStrict_ACU(tableData) {\n        const engine = new SqliteEngine();\n        const syncBridge = new SyncBridge(en"
[tableData] "try {\n            await engine.init();\n            syncBridge.loadFromTableData(tableData, { strict: true });\n        }\n        finally {\n            engine.dispose();\n        }\n  "
[tableData] "pose();\n        }\n    }\n    async function validateSqliteTemplateDataStrict_ACU(tableData) {\n        try {\n            await hydrateTableDataStrict_ACU(tableData);\n            retu"
[tableData] "ict_ACU(tableData) {\n        try {\n            await hydrateTableDataStrict_ACU(tableData);\n            return { success: true };\n        }\n        catch (error) {\n            retu"
[sheetData] "eginning with \"sheet_\".' };\n        }\n        if (!isObjectRecord_ACU$1(options.sheetData)) {\n            return { error: `V2 sheet checkpoint requires object sheetData for ${optio"
[sheetData] ".sheetData)) {\n            return { error: `V2 sheet checkpoint requires object sheetData for ${options.sheetKey}.` };\n        }\n        if (!options.reason) {\n            return {"
[sheetData] "validation.error };\n        const normalizedSheetData = deepClone_ACU$1(options.sheetData);\n        const normalization = normalizeCanonicalTableRows_ACU({ [options.sheetKey]: norm"
[sheetData] "upShards_ACU(options) {\n        const requestedEntries = Object.entries(options.sheetDataByKey || {})\n            .filter(([sheetKey]) => sheetKey.startsWith('sheet_'));\n        if"
[guideData] "eedRows = getEffectiveSeedRowsForSheet_ACU(String(sheet.uid), {\n                guideData: null,\n                allowTemplateFallback: true,\n            });\n            if (Array."
[guideData] "unction commitCurrentFloorTemplateScopeOnly_ACU(options) {\n        if (!options.guideData || typeof options.guideData !== 'object' || Array.isArray(options.guideData)) {\n          "
[guideData] "mplateScopeOnly_ACU(options) {\n        if (!options.guideData || typeof options.guideData !== 'object' || Array.isArray(options.guideData)) {\n            return { saved: false, err"
[guideData] "ons.guideData || typeof options.guideData !== 'object' || Array.isArray(options.guideData)) {\n            return { saved: false, error: 'scope-only 模板提交必须提供有效的 guideData。' };\n     "
[表格数据] "     {\n            \"role\": \"USER\",\n            \"content\": \"你是【填表AI】，负责根据用户提供的资料对表格数据执行增删改操作。\\n\\n## 核心任务\\n依据三类资料来源执行表格编辑：\\n- <背景设定>：故事及人物设定\\n- <正文数据>：上轮发生的故事\\n- <当前表格数据>：之前的数据作为填表基础"
[表格数据] "据执行增删改操作。\\n\\n## 核心任务\\n依据三类资料来源执行表格编辑：\\n- <背景设定>：故事及人物设定\\n- <正文数据>：上轮发生的故事\\n- <当前表格数据>：之前的数据作为填表基础\\n\\n## 输出格式（严格执行）\\n\\n<thought>\\n[分析当前轮次的剧情变化]\\n[阅读所有填表相关规则]\\n[根据填表规则确定需要修改的表格和字段]\\n"
[表格数据] "次的剧情变化]\\n[阅读所有填表相关规则]\\n[根据填表规则确定需要修改的表格和字段]\\n[逐步推理每个修改操作，说明理由]\\n针对纪要表的额外规则：如果<当前表格数据>里存在纪要表，那么本轮就必须对其进行插入一条新的总结记录。\\n日志与纪要语气校准：你在思考纪要时，必须区分“正常恋爱互动”与“暗黑主从文风”。你可以使用正常的交流词汇（如：提议、要求、同意、"
[表格数据] "           \"content\": \"现在请按照我的要求立刻开始你的工作 \\n========================\\n\\n以下是当前的<当前表格数据>,记录有本轮之前的数据，你的一切操作指令都必须在这个<当前表格数据>的基础与指导上进行：\\n<当前表格数据>\\n$0\\n</当前表格数据>\\n\\n$8\",\n            \"dele"
[plotSettings] "tatus_full`;\n    const STORAGE_KEY_PLOT_SETTINGS_ACU = `${SCRIPT_ID_PREFIX_ACU}_plotSettings`;\n    // ═══════════════════════════════════════════════════════════════\n    // Profile"
[plotSettings] "Run) {\n        if (dryRun)\n            return false;\n        if (!settings_ACU?.plotSettings?.enabled)\n            return false;\n        if (isQuietLikeGeneration_ACU(type, params)"
[plotSettings] "   standardizedTableFillEnabled: true,\n        toastMuteEnabled: false,\n        plotSettings: JSON.parse(JSON.stringify(DEFAULT_PLOT_SETTINGS_ACU)),\n        plotPresetBindings: {},"
[plotSettings] "ontent);\n        return base;\n    }\n    function getLegacyPlotPromptContent_ACU(plotSettings, promptId) {\n        try {\n            const p = plotSettings?.prompts;\n            if "
[占位符] "ay(src) ? JSON.parse(JSON.stringify(src)) : [];\n        // 如果提供了主内容，替换 $CONTENT 占位符\n        if (mainContent) {\n            base.forEach((item) => {\n                if (item.content"
[占位符] "+参数；seedRows 仅保留字段，不默认展开到 content）\n        // - 否则返回 null，让调用方按旧逻辑处理（例如用完整模板结构作为占位符）\n        if (foundCount <= 0) {\n            if (hasSheetGuide) {\n                // 直接物化：仅表头（see"
[占位符] "L 中的中文名替换为英文名（跳过字符串值）\n         *\n         * 安全替换策略：\n         * 1. 先把单引号字符串提取出来，用占位符替代\n         * 2. 在安全的 SQL 上做中文→英文替换（长名称优先，避免子串误匹配）\n         * 3. 把字符串值放回去\n         */\n        tra"
[占位符] "{\n            if (!sql)\n                return sql;\n            // 1. 提取单引号字符串，用占位符替代\n            const strings = [];\n            let safeSql = sql.replace(/'[^']*'/g, (match) => {"
[替换] "           initNode: \"游戏初始化时，根据设定为主角添加初始技能。\",\n            deleteNode: \"技能因剧情被剥夺或替换时删除。\\nSQL示例: DELETE FROM protagonist_skills WHERE skill_name = '被剥夺的技能';\",\n            updateNode:"
[替换] "olor='#ffc107';\">${escapeHtml_ACU$1(label)}</button>`;\n    }\n    /**\n     * 生成正文替换 toast 中的\"重新优化\"按钮 HTML\n     */\n    function renderReoptButton_ACU() {\n        return `<button id=\""
[替换] "}...\"`);\n                }\n            }\n        }\n        logDebug_ACU(`[正文优化] 替换统计: 成功 ${appliedCount}/${optimizations.length}，失败 ${failedCount}`);\n        if (failedItems.length"
[替换] " Array.isArray(src) ? JSON.parse(JSON.stringify(src)) : [];\n        // 如果提供了主内容，替换 $CONTENT 占位符\n        if (mainContent) {\n            base.forEach((item) => {\n                if ("
