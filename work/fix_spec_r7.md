# R7 修复规格 —— 六版 BATTLE 预设（只读规格，执行前通读）

> 执行方式：对 `C:\Users\zouyu\Downloads\酒馆\数据库\剧情推进预设\Cirno_BATTLE_Turn_*.json` 做精确字符串替换。
> 参考可读文本：`C:\Users\zouyu\Downloads\BATTLE_work\review_dump\Cirno_BATTLE_Turn_*.txt`（dump 行号即下文 L 号）。
> 替换发生在 JSON 中 plotTask 各消息 content 与任务 description 字段。OLD 必须与 dump 精确一致（含全角/半角、emoji、{{user}}、<tag>），先 grep 确认存在再替换。替换后 JSON.parse 必须成功、顶层必须仍是数组（首字符 [ ）。
> 备份已由主代理完成（.bak-pre-r7 已归档 备份\），执行者不需要再备份，直接改源 JSON。

## 1. 全局语义变更（P0-C）：黄毛胜·终局不踢出（六版通用，最高优先级）

用户新指令（2026-08-02）：黄毛胜利后**不会**彻底从黄毛列表踢出——黄毛仍留在追踪中、仍可刷新判定互动；女主角仍可在 {{user}} 身边活动、与 {{user}} 互动；只是与黄毛确认了最终关系（夫妻级），亲密互动只与黄毛发生、黄毛不多介入 {{user}} 的日常生活。观感服务于绿帽向读者（对象名义上还在身边、实际亲密已属黄毛）。

### G1. S2-MSG4 线状态判定规则「黄毛胜·终局」行（六版，行号：straight L435 / FT L445 / DEI L456 / straight_NTRS 同段 / FT_NTRS / DEI_NTRS）
- OLD（straight 版原文，其它版同句但注意 FT/DEI 前后多一行后宫线）：
  `> - **黄毛胜·终局**：本轮确认黄毛胜（剧情确认：对象明确选择黄毛——接受表白/确立关系/成婚）——该对象线终局锁定（对象与 {{user}} 封顶好朋友、与黄毛成真夫妻），后续轮不再判定。`
- NEW：
  `> - **黄毛胜·终局**：本轮确认黄毛胜（剧情确认：对象明确选择黄毛——接受表白/确立关系/成婚）——该对象线终局锁定：对象与 {{user}} 封顶好朋友、与黄毛成真夫妻级亲密关系。**黄毛不踢出追踪**：仍列入【黄毛动向追踪】，后续轮仍可刷新判定互动（spawn/no_spawn/act 照常判定），黄毛与对象的夫妻级亲密互动戏可持续编排；对象仍可在 {{user}} 身边活动、与 {{user}} 正常互动，但亲密互动只与黄毛发生，黄毛不多介入 {{user}} 的日常生活。`

### G2. S2-MSG4 追踪区块「不再列出」句（六版，straight L436 / FT L446 / DEI L457 / NTRS 三版对应行）
- OLD straight / NTRS 三版（同一句）：
  `；黄毛胜·终局/线闭合的对象不再列出）`
- OLD FT：`；黄毛胜·终局的对象不再列出；**黄毛败·友好（男娘系天意待触发）仍须列出**，标「天意待触发」供 S3 编排投怀戏）`
- OLD DEI：`；黄毛胜·终局的对象不再列出；**黄毛败·友好：男娘系（天意待触发）仍须列出**、标「天意待触发」供 S3 编排投怀戏，**正常男性则线闭合不再列出**）`
- NEW（straight/NTRS 三版）：`；黄毛胜·终局的对象**仍须列出**（线锁定非闭合，标线状态供 S3 编排夫妻级关系戏）；仅线闭合（黄毛败·友好/彻底离场）不再列入行动判定）`
- NEW（FT）：`；黄毛胜·终局的对象**仍须列出**（线锁定非闭合，标线状态供 S3 编排夫妻级关系戏）；**黄毛败·友好（男娘系天意待触发）仍须列出**，标「天意待触发」供 S3 编排投怀戏；仅彻底离场不再列入行动判定）`
- NEW（DEI）：`；黄毛胜·终局的对象**仍须列出**（线锁定非闭合，标线状态供 S3 编排夫妻级关系戏）；**黄毛败·友好：男娘系（天意待触发）仍须列出**、标「天意待触发」供 S3 编排投怀戏，**正常男性则线闭合不再列出**；仅彻底离场不再列入行动判定）`

### G3. S2-MSG4 刷新状态两档（P0-A，六版）
- G3a. spawn ② 行（straight L357 / FT L365 / DEI L373 / NTRS 三版对应行）：
  - OLD：`② 分支A——已有追踪黄毛（该目标已绑定黄毛），本轮把该黄毛动向列入【黄毛动向追踪】（标签内不重写人设，须补列动向+线状态+五型+型体概要，与追踪区块格式一致），黄毛在场或离场均可为 spawn（离场=追踪其去向，仍可行动）。`
  - NEW：`② 分支A——已有追踪黄毛（该目标已绑定黄毛），本轮把该黄毛动向列入【黄毛动向追踪】（标签内不重写人设，须补列动向+线状态+五型+型体概要，与追踪区块格式一致）。**spawn=本轮黄毛在当前场景在场（或本轮新刷新登场）；黄毛不在当前场景（含离场追踪/场景外行动）=no_spawn**——no_spawn 不等于 no-act，离场黄毛仍可判 act（见下方 no_spawn ②）。`
- G3b. no_spawn 头注（straight L358 / FT L366 / DEI L374 / NTRS 三版）：
  - OLD：`- **no_spawn**：本轮无黄毛在场/无活跃可行动黄毛。两种情形：`
  - NEW：`- **no_spawn**：本轮无黄毛在当前场景在场（追踪中/离场黄毛仍可能行动）。两种情形：`
- G3c. no_spawn ② 行（straight L360 / FT L368 / DEI L376 / NTRS 三版对应行）：
  - OLD straight：`② 分支A——已有追踪黄毛但该黄毛线已闭合（黄毛胜·终局/黄毛败·友好）或黄毛彻底离场再无行动可能（如黄毛远走他乡且无尾随目标途径）；若无活跃追踪黄毛则下游 stage3 走快速通道。`
  - NEW straight：`② 分支A——已有追踪黄毛：黄毛不在当前场景（离场追踪/场景外行动）——若黄毛与对象均不在 {{user}} 当前场景但两者可接触（黄毛离场前往对象所在处攻略），仍可判 act（场景外行动，发生在 {{user}} 场景外）；若黄毛线已闭合（黄毛败·友好）或黄毛彻底离场再无行动可能（如黄毛远走他乡且无尾随目标途径）→ no-act。行动判定为 no-act 时下游 stage3 走快速通道。`
  - OLD FT/DEI（含男娘系特判句，注意 FT/DEI 该行把男娘系特判放在线闭合句之后）：`② 分支A——已有追踪黄毛但该黄毛线已闭合（黄毛胜·终局）或黄毛彻底离场再无行动可能（如黄毛远走他乡且无尾随目标途径）；**男娘系黄毛败·友好（天意待触发）不算彻底闭合——黄毛仍在场以朋友身份与对象相处并酝酿对 {{user}} 的爱意，判 spawn 并推进投怀戏**；若无活跃追踪黄毛则下游 stage3 走快速通道。`
  - NEW FT/DEI：`② 分支A——已有追踪黄毛：黄毛不在当前场景（离场追踪/场景外行动）——若黄毛与对象均不在 {{user}} 当前场景但两者可接触（黄毛离场前往对象所在处攻略），仍可判 act（场景外行动，发生在 {{user}} 场景外）；若黄毛线已闭合（黄毛败·友好）或黄毛彻底离场再无行动可能（如黄毛远走他乡且无尾随目标途径）→ no-act。**男娘系黄毛败·友好（天意待触发）不算闭合——黄毛仍在场以朋友身份与对象相处并酝酿对 {{user}} 的爱意，按在场处理（spawn）并供 S3 推进投怀戏**。行动判定为 no-act 时下游 stage3 走快速通道。`
  - NTRS 三版 no_spawn ② 行：见对应版 dump（结构与 straight 相同：`② 分支A——已有追踪黄毛但该黄毛线已闭合（黄毛胜·终局/黄毛败·友好）或黄毛彻底离场再无行动可能（如黄毛远走他乡且无尾随目标途径）；若无活跃追踪黄毛则下游 stage3 走快速通道。`，其中 NTRS 版的闭合枚举可能为「黄毛胜·终局」；按 dump 精确 OLD，替换为与 straight 相同的 NEW 语义：黄毛不在当前场景仍可判 act（场景外），线闭合仅指黄毛败·友好/彻底离场，行动 no-act 才走快速通道）。

### G4. S2-MSG4 TRIGGER RULES 0 / 1（P0-C，六版）
- G4a. TRIGGER 0（straight L509 / FT L519? / DEI L529? / NTRS 三版对应行；注意 FT/DEI 行号不同，按 dump 精确取）：
  - OLD（straight）：`0. **本轮无任何已真正锁定的活跃黄毛**（含：本轮 no_spawn 且无历史锁定；黄毛胜·终局的对象不再判定）→ 直接判 <thugAction>no-act</thugAction>。注意：仅 no_spawn 不等于 no-act——若有上轮已真正锁定的活跃黄毛，仍须进入后续规则判定 act/no-act。`
  - NEW：`0. **本轮无任何已真正锁定的活跃黄毛**（含：本轮 no_spawn 且无历史锁定）→ 直接判 <thugAction>no-act</thugAction>。注意：仅 no_spawn 不等于 no-act——追踪中的活跃黄毛（含黄毛胜·终局的对象）仍须进入后续规则判定 act/no-act。`
  - FT/DEI 的 TRIGGER 0 可能不含「黄毛胜·终局的对象不再判定」子句（FT 子代理报 L519/L531 上下文），按 dump 实际文本，若 OLD 中无该子句则只替换存在的部分（若整句与 straight 不同则把「黄毛胜·终局的对象不再判定」子句删除即可）。
- G4b. TRIGGER 1（straight L510 / FT / DEI / NTRS 三版）：
  - OLD（straight）：`1. **线状态=黄毛败·友好/黄毛胜·终局**（线已闭合）的对象：黄毛不再有行动判定——此类对象本轮一律 <thugAction>no-act</thugAction>（黄毛退居朋友位，不参与竞争戏）。`
  - NEW（straight）：`1. **线状态=黄毛败·友好**（线闭合）的对象：黄毛退居朋友位、不再有竞争行动判定——此类对象本轮一律 <thugAction>no-act</thugAction>。**线状态=黄毛胜·终局**的对象：黄毛不踢出追踪、仍可判定行动——黄毛与对象的夫妻级亲密互动戏（{{user}} 在场/不在场按知情度档位）可判 act，黄毛不多介入 {{user}} 日常生活。`
  - FT/DEI 的 TRIGGER 1 可能含「后宫线」对象行（后宫互动判定）与黄毛败·友好/终局行——按 dump 精确 OLD，把「黄毛胜·终局」从闭合枚举中拆出（不再一律 no-act），并保留后宫线行不动。
  - NTRS 三版的 TRIGGER 1 含「线状态=NTRS期 → 恢复原 NTRS 判定逻辑」——保留 NTRS期行，把「黄毛胜·终局」从 no-act 枚举中拆出（同 straight NEW 语义）。

### G5. S2-MSG4 胜负确认轮例外（P0-C，六版）
- OLD（straight L511）：`> **胜负确认轮例外**：但本轮若刚确认胜负（thugActionReason 写明「雄竞结果：黄毛胜/黄毛败」）则例外判 act，以触发下游 S3 的终局/收尾场景全量编排；胜负确认后的后续轮才回归 no-act。`
- NEW（straight）：`> **胜负确认轮例外**：但本轮若刚确认胜负（thugActionReason 写明「雄竞结果：黄毛胜/黄毛败」）则例外判 act，以触发下游 S3 的终局/收尾场景全量编排；黄毛败·友好的后续轮回归 no-act（线闭合）；黄毛胜·终局的后续轮**不**回归 no-act——黄毛仍按追踪判定互动（夫妻级亲密戏可持续，见规则 1）。`
- FT（L521/L523 分两条写黄毛胜/黄毛败）：按 dump 精确 OLD，分别把「胜负确认后的后续轮才回归 no-act」改为「黄毛败·友好的后续轮回归 no-act（线闭合）；黄毛胜·终局的后续轮不回归 no-act——黄毛仍按追踪判定互动」。
- DEI（L535/L537 同 FT 分两条）同理。
- NTRS 三版（单条，同 straight L525 附近）：同 straight NEW；注意 NTRS 版黄毛败后转 NTRS期（淫妻线），「黄毛败·友好的后续轮回归 no-act」应改为「黄毛败（转 NTRS期）的后续轮按 NTRS 期判定逻辑正常判定（淫妻线推进），不回归 no-act」。

### G6. S3-MSG0 act 编排「黄毛胜·终局→」句（P0-C，六版）
- OLD（straight L624 尾部）：`）；黄毛胜·终局→终局场景（对象与黄毛成真夫妻、与 {{user}} 封顶好朋友）；黄毛败·友好→友情收尾场景（对象与黄毛成为好朋友、{{user}} 与对象恋爱确立，黄毛退出竞争）。`
- NEW（straight）：`）；黄毛胜·终局→终局锁定+关系戏（对象与黄毛成真夫妻、与 {{user}} 封顶好朋友；黄毛仍在追踪、仍可行动——与对象的夫妻级亲密互动戏可持续，对象仍可在 {{user}} 身边活动互动但亲密只属黄毛，黄毛不多介入 {{user}} 生活）；黄毛败·友好→友情收尾场景（对象与黄毛成为好朋友、{{user}} 与对象恋爱确立，黄毛退出竞争）。`
- FT/DEI（L641/L655 尾部，后面还有「后宫线→后宫互动场景」）：OLD 尾部为 `）；黄毛胜·终局→终局场景（对象与黄毛成真夫妻、与 {{user}} 封顶好朋友）；黄毛败·友好→友情收尾场景（对象与黄毛成为好朋友、{{user}} 与对象恋爱确立，黄毛退出竞争）；后宫线→后宫互动场景（男娘系黄毛与 {{user}} 的亲近/陪伴/暧昧推进，{{user}} 被动收后宫）。`——仅把「黄毛胜·终局→终局场景（对象与黄毛成真夫妻、与 {{user}} 封顶好朋友）」替换为「黄毛胜·终局→终局锁定+关系戏（对象与黄毛成真夫妻、与 {{user}} 封顶好朋友；黄毛仍在追踪、仍可行动——与对象的夫妻级亲密互动戏可持续，对象仍可在 {{user}} 身边活动互动但亲密只属黄毛，黄毛不多介入 {{user}} 生活）」，其余不动。另按子代理报告 FT/DEI 该句「黄毛败·友好」部分未提投怀（P1：后宫线触发不了）——把 FT/DEI 的 `黄毛败·友好→友情收尾场景（对象与黄毛成为好朋友、{{user}} 与对象恋爱确立，黄毛退出竞争）` 替换为 `黄毛败·友好→友情收尾场景（对象与黄毛成为好朋友、{{user}} 与对象恋爱确立，黄毛退出竞争；男娘系黄毛败·友好同时推进天意·后宫线——酝酿对 {{user}} 的爱意，为投怀/入后宫铺垫）`。
- NTRS 三版（L640/L649/L661 附近，黄毛胜·终局句同 straight 语义）：OLD 尾部 `黄毛胜·终局→终局场景（对象与黄毛成真夫妻、与 {{user}} 封顶好朋友）`，替换为「黄毛胜·终局→终局锁定+关系戏（对象与黄毛成真夫妻、与 {{user}} 封顶好朋友；黄毛仍在追踪、仍可行动——与对象的夫妻级亲密互动戏可持续，对象仍可在 {{user}} 身边活动互动但亲密只属黄毛，黄毛不多介入 {{user}} 生活）」。

### G7. S3-MSG2 状态机「黄毛胜·终局」定义段（P0-C，六版）
- OLD（straight L636）：`- **黄毛胜·终局**：黄毛胜（剧情确认对象明确选择黄毛——接受表白/确立关系/成婚）——该对象线**终局锁定**：对象与 {{user}} 关系封顶「好朋友」，与黄毛成为真正夫妻级亲密关系。该对象后续轮不再参与判定，{{user}} 转攻其他可攻略对象。`
- NEW（straight）：`- **黄毛胜·终局**：黄毛胜（剧情确认对象明确选择黄毛——接受表白/确立关系/成婚）——该对象线**终局锁定**：对象与 {{user}} 关系封顶「好朋友」，与黄毛成为真正夫妻级亲密关系。**黄毛不踢出追踪**：仍列入【黄毛动向追踪】、仍可判定互动——黄毛与对象的夫妻级亲密互动戏可持续编排（暗线/明线按知情度档位），对象仍可在 {{user}} 身边活动、与 {{user}} 正常互动（好朋友日常），但亲密互动只与黄毛发生，黄毛不多介入 {{user}} 的日常生活；{{user}} 可转攻其他可攻略对象，也可保持与对象的日常互动。`
- FT/DEI（L456? / L445? 附近，结构与 straight 同）：同 NEW。注意 DEI 状态机有「后宫线」段（保持不动），仅替换黄毛胜·终局段。
- NTRS 三版（S3-MSG2 状态机含「雄竞期/NTRS期/黄毛胜·终局」三段）：替换「黄毛胜·终局」段为同 NEW 语义（NTRS 版开头按 dump 精确 OLD，可能写作「黄毛胜·终局：黄毛胜（剧情确认…）——该对象线终局锁定：对象与 {{user}} 关系封顶「好朋友」…该对象后续轮不再参与判定，{{user}} 转攻其他可攻略对象。」，替换掉「该对象后续轮不再参与判定」部分为新语义）。

### G8. S3-MSG2 黄毛登场角色门：补「spawn+黄毛胜·终局」行（P0-C，六版）
- straight（L707-710 三行）在 spawn+黄毛败·友好 行后、no_spawn 行前插入一行：
  ` - thugSpawn 状态=spawn 且线状态=黄毛胜·终局 → 黄毛可写入登场名单（标注"对象的情人/丈夫·[五型]·终局"）——黄毛与对象的夫妻级亲密关系戏按剧情编排；若本轮为场景外行动则不列入登场名单（见场景外标注规则）。`
- FT/DEI（登场门有雄竞期/败·友好/后宫线/no_spawn 行，子代理报告缺终局行）：同样在 no_spawn 行前插入上述终局行。
- NTRS 三版（登场门有 spawn+雄竞期 / spawn+NTRS期 / no_spawn 行）：同样在 no_spawn 行前插入终局行（标注"对象的情人/丈夫·[五型]·终局"）。

### G9. S3-MSG2 no_spawn 登场门行（P0-D，六版）
- OLD（straight L710 / FT L732 / DEI L745 / NTRS 三版对应行）：` - thugSpawn 状态=no_spawn → 本轮无新黄毛登场；prologue 登场名单不列入新黄毛；但追踪中的活跃黄毛（雄竞期）仍按对应线状态规则登场——即使目标不在场，黄毛尾随/赶赴/暗中行动的戏照常编排。`
- NTRS 三版的该行括号内为（雄竞期/NTRS期）：`但追踪中的活跃黄毛（雄竞期/NTRS期）仍按对应线状态规则登场——即使目标不在场，黄毛尾随/赶赴/暗中行动的戏照常编排。`
- NEW（六版通用，NTRS 版保留（雄竞期/NTRS期）括号）：` - thugSpawn 状态=no_spawn → 本轮无新黄毛登场；prologue 登场名单不列入新黄毛；追踪中的活跃黄毛若本轮 act 且行动发生在 {{user}} 当前场景内，按对应线状态规则登场编排；若黄毛与对象均在 {{user}} 当前场景之外（场景外行动），该戏仅写入 stage（标注「场景外场景」），prologue 不展开、黄毛不列入登场名单。`

### G10. P2：「明确且长期拒绝」措辞去数值化残留（六版多处，逐一替换）
出现位置（每版 3-5 处）：
1. S2 description（各版 L264 行）：「黄毛败=对象明确且长期拒绝黄毛/明确选择{{user}}」→「黄毛败=综合判断女主行为已选择{{user}}（对两人的态度/行为/话语倾向{{user}}）」（NTRS 版后半句保留「（对象转入NTRS期…）」）。
2. S2-MSG4 胜负判定段（straight L472 / FT L482 / DEI L493 / NTRS 三版 L487/L494/L505）：「对象明确且长期拒绝黄毛/明确选择 {{user}}」→「对象的行为综合判断已选择 {{user}}（对两人的态度/行为/话语倾向 {{user}}）」。
3. S3-MSG2 线状态定义（straight L635 / FT L652 / DEI L666 / NTRS 三版 L651/L660/L672）：「黄毛败（对象明确且长期拒绝黄毛 / 明确选择 {{user}}）」→「黄毛败（综合判断女主行为已选择 {{user}}——对两人的态度/行为/话语倾向 {{user}}）」。
4. S3 剧情驱动推进段（straight L1017 / FT L1042 / DEI L1055 / NTRS 三版 L1080/L1096/L1107）：「对象明确选择/明确长期拒绝」→「对象明确选择/综合判断已倾向 {{user}}」；「（对象明确选择/明确长期拒绝）」→「（对象明确选择/综合判断女主行为已倾向 {{user}}）」。
5. S3 sparkNotes 胜负判定（straight L1145 / FT L1171 / DEI L1185 / NTRS 三版 L1223/L1239/L1251）：「对象是否明确且长期拒绝黄毛或明确选择 {{user}}」→「女主的行为是否已选择 {{user}}（综合对两人的态度/行为/话语判断）」。
> 注意：NTRS 版的「41%」「察觉型」「+0~5%」等是淫妻线合法数值，保留不动；「无淫妻癖」等否定句式是合法表述，保留不动。只替换「明确且长期拒绝」「明确长期拒绝」「明确且长期」这类措辞。

### G11. P2：S3-MSG7 stage 模板「NTR 标记列」→「关系标记列」（六版）
- OLD（六版相同，straight L851 / FT L876 / DEI L889 / NTRS 三版对应行）：`{{user}} 在场时在 NTR 标记列加注 👁️`
- NEW：`{{user}} 在场时在关系标记列加注 👁️`

### G12. P2：S3-MSG0 快速通道恢复条件「spawn 或 act」→「act」（六版）
- OLD（straight L602 / FT / DEI / NTRS 三版）：`此规则仅为节省等待时间，不影响后续任何轮次——下一轮若有 spawn 或 act，恢复完整导演分析。`
- NEW：`此规则仅为节省等待时间，不影响后续任何轮次——下一轮若 thugAction=act，恢复完整导演分析。`

### G13. P2：S3-MSG11 活人感「强度≥6每轮只衰减1-2点」→ 定性（纯雄竞三版 straight L1024 / FT L1050 / DEI L1063 附近；NTRS 三版如有同句也一并改）
- 按 dump 精确 OLD（例如 `情绪惯性：强度≥6每轮只衰减1-2点` 或相近句子），替换为定性描述：`情绪惯性：强烈情绪自然缓释（不设数值阈值，按剧情节奏衰减）`。

### G14. P2：S3-MSG0「【黄毛刷新状态】」行 no_spawn 定义简化（六版）
- OLD（straight L608 / FT L625 / DEI L639 / NTRS 三版）：`【黄毛刷新状态】spawn=本轮有黄毛在场/在追踪 / no_spawn=本轮无黄毛；`
- NEW：`【黄毛刷新状态】spawn=本轮有黄毛在当前场景在场/新刷新登场 / no_spawn=本轮无黄毛在当前场景在场（追踪中黄毛仍列出，可能场景外行动）；`

### G15. P0-B：NTRS 三版表格权威源降级 + 删重复句（仅 straight_NTRS / FT_NTRS / DEI_NTRS 三版）
> 背景：NTRS 三版 S2-MSG0 说「判定任务不读表格」，但 S2-MSG2 又说「表格为权威源、查表判断已有黄毛」；S3-MSG0 说「不在任何表格里找」，S3-MSG15 又说「查询数据库表格获取权威数据」。三表注入（{[db.黄毛表.get()]} 等）保留（T40 已落地），但「表格=权威源」表述降级为「表格=设定参考，状态以追踪区块为准」。
- G15a. S2-MSG2 表格权威源段（straight_NTRS L328-331 / FT_NTRS L333-336 / DEI_NTRS L342-345，均为三句重复+矛盾，按各版 dump 精确 OLD 整段替换）：
  - OLD 模式（三句）：
    第1句：`本轮查询数据库表格获取权威数据，并配合黄毛追踪机制使用：`
    第2句：`- **表格为权威源**：黄毛表（锁定对象/lock_status/进度条/型体设定/性别类型/五型）、重要角色表（登场角色设定）、NTRS备忘录（长期备忘）——查表判断已有黄毛与沿用设定；`
    第3句（重复）：`本轮查询数据库表格获取权威数据（黄毛表为黄毛条目/型体设定/性器官规则/进度权威源；重要角色表为登场角色设定；NTRS备忘录为长期备忘）：`
  - NEW（三段合一，替换上述三句为一段）：
    `本轮查询数据库表格获取黄毛档案数据（黄毛表为型体设定/性器官规则/NTRS期进度；重要角色表为登场角色设定；NTRS备忘录为长期备忘），仅作设定/人设参考，不用于判断已有黄毛：
    - **黄毛动向追踪为跨轮状态权威**：每轮把场上每个已刷新黄毛的动向（在场/离场/尾随目标/暗中布局）、线状态（雄竞期/NTRS期/黄毛胜·终局）写进 <thugSpawn> 的【黄毛动向追踪】区块——**已有黄毛、锁定与线状态一律以追踪区块为准，不查表判断**。**目标离场不意味着黄毛停摆**：黄毛可尾随/赶赴/潜伏接近目标所在处继续行动。`
  - 注意：替换后其后的表格注入块（`<黄毛表当前条目>{[db.黄毛表.get()]}</黄毛表当前条目>` 等三块）保留不动。
- G15b. S3-MSG15 表格权威源段（straight_NTRS L1128-1129 / FT_NTRS L1144-1145 / DEI_NTRS L1155-1156，两行重复）：
  - OLD 两行：
    第1行：`本轮查询数据库表格获取黄毛档案权威数据（黄毛表为型体设定/性器官规则/进度权威源；重要角色表为登场角色设定；NTRS备忘录为长期备忘），配合 <thugSpawn> 追踪区块使用：`
    第2行（重复）：`本轮查询数据库表格获取权威数据（黄毛表为黄毛条目/型体设定/性器官规则/进度权威源；重要角色表为登场角色设定；NTRS备忘录为长期备忘）：`
  - NEW（两行合一）：`本轮查询数据库表格获取黄毛档案数据（黄毛表为型体设定/性器官规则/NTRS期进度；重要角色表为登场角色设定；NTRS备忘录为长期备忘），仅作设定/人设参考，配合 <thugSpawn> 追踪区块使用——运行期线状态/锁定/型体概要/性器官规则以追踪区块与历史刷新记录为准，不从前文猜、不查表判断状态：`
  - 注意：其后的表格注入块三块保留不动；其后的 L1142（straight_NTRS）「沿用追踪黄毛…以历史刷新记录为唯一权威源，不从前文猜。」句保留（已符合新语义，无需改）。
- G15c. P0-B 进度条残留（NTRS 三版 S2-MSG2 与 S3-MSG15）：NEW 文案中已把「进度权威源」改为「NTRS期进度」并限定「不用于判断已有黄毛」——雄竞期胜负不得读取进度数值；若 dump 中另有单独句子提到「进度权威源」已被 G15a/G15b 整段替换覆盖，无需单独处理。

## 2. 版本特有项（按版执行）

### S1. straight（纯雄竞）
- S1a. P2-1：S2-MSG4 HARD RULE 1（straight L366）「线已闭合的对象（黄毛胜·终局/黄毛败·友好，或NTRS版已转NTRS期）视为仍绑定」——straight 版不应出现「或NTRS版已转NTRS期」残留：`，或NTRS版已转NTRS期` 删除，替换为 `（黄毛胜·终局/黄毛败·友好）视为仍绑定、不参与刷新、不误判为未绑定`（注意该句与 G1 新语义的衔接：黄毛胜·终局的对象已绑定黄毛故不换新黄毛，此「不参与刷新」指不刷新新黄毛，保留）。
- S1b. P2-5：S3-MSG17 自检8（straight L1124）「<plot> 内是否出现 <ntrsProgress>…」→ 本版无 ntrsProgress 标签：把 `<ntrsProgress>` 字样改为 `任何进度标签`（若句子结构为「是否出现 <ntrsProgress>」则整句改「是否出现任何进度标签」）。
- S1c. P2-7 追踪区块字段缺口（straight L427 分支A 追踪格式）：示例行 `型体概要=[1句，如"外表温和清秀，性器官勃起时足够粗长持久"]` 已内嵌性器官；S3 要求「型体概要/性器官规则」双字段——在追踪格式示例行后补一句：`；性器官规则=[1句，如"勃起时足够粗长持久，未勃起时不明显"]`（插到该示例行末尾「...持久"]` 之前或之后，按 dump 实际行精确插入，使追踪区块显式含性器官字段）。
- S1d. P2-8 场景外×知情度未联动（straight L624 / L884）：场景外 act 必为用户不在场，知情度只能 📹/🌙——在 S3-MSG0 场景外标注句（L624「…stage 须标注「场景外场景」，prologue 不展开该场景外戏」）之后追加：`（场景外行动 {{user}} 必不在场，{{user}} 知情度不得为👁️，只能 📹 事后知情或 🌙 完全不知）`；同 S3 stage 模板「场景外标注」行（L884 附近）末尾追加同句。
- S1e. P2-9 胜负确认轮时序（straight L472 / L511）：在 S2-MSG4 胜负判定段（L472「胜负一经确认…thugSpawn 直接标新线状态」）补一句界定确认轮：`（确认轮=胜负事件于本轮输入/剧情中新出现的那一轮；上轮已确认的胜负，本轮为后续轮）`。
- S1f. P1-1 分支A/B 门控多黄毛不可达（straight L340/L341）：把分支B 定义（L341）`【分支 B — 无追踪黄毛】：场上尚无任何已刷新黄毛（或所有黄毛均已终局闭合），走"黄毛刷新判定"逻辑判定本轮是否为某💔可攻略角色刷新一个新黄毛。` 改为 `【分支 B — 有待刷新目标】：场上存在**尚未绑定黄毛**的💔可攻略目标（无论场上是否已有其他黄毛在追踪），对其走"黄毛刷新判定"逻辑判定本轮是否为该目标刷新一个新黄毛；已有追踪黄毛的目标走分支A 追踪写法。`（分支A 定义保留「已绑定目标不再换新黄毛」，但删「一气到底不再刷新新黄毛」中的全局含义——将分支A（L340）中 `存在即视为"该目标已绑定黄毛"，一气到底不再刷新新黄毛，改为把该黄毛本轮动向列入` 改为 `存在即视为"该目标已绑定黄毛"，该目标不再刷新新黄毛，改为把该黄毛本轮动向列入`。FT/DEI 分支A/B 同改。）

### S2. FT（纯雄竞）
- S2a. P0：S2-MSG4 no_spawn ② 内嵌「判 spawn」（FT L368）——已在 G3c 整体替换覆盖。
- S2b. P1：S3-MSG5 prologue 登场门 no_spawn 行（FT L732）——已在 G9 整体替换覆盖。
- S2c. P1：S3-MSG0 act 编排·黄毛败·友好未提投怀（FT L641）——已在 G6 尾部替换覆盖（补「男娘系黄毛败·友好同时推进天意·后宫线…」）。
- S2d. P1：S3-MSG5 登场门「或淡出」（FT L730 附近，spawn+黄毛败·友好 行）`…或淡出——按剧情自然，不作竞争角色登场。` → 改为 `…按剧情自然，不作竞争角色登场（男娘系黄毛须留在名单中供投怀戏编排）。`（删「或淡出」绝对化，男娘系必须登场推进投怀）
- S2e. P2：「男娘系」涵盖假小子（FT L298/L302/L368 附近）——后宫线触发表述 `男娘系黄毛（本版全部黄毛均为伪娘/药娘/假小子）` 中「男娘系」字面与假小子冲突：改为 `伪娘系黄毛（伪娘/药娘/假小子三型）`（凡「男娘系」用于指代本版三型黄毛处统一改「伪娘系」，但「天意·后宫线」「男娘系黄毛」作为 FT 后宫线机制名保留——仅把「男娘系黄毛（…均为伪娘/药娘/假小子）」这类括注中的指代改为「伪娘系」）。
- S2f. P2：S3-MSG0 Log 行（FT L618）`Log：仅一行「no-act，快速通道输出」（本版无进度标签，不涉及进度省略）` → 删后半句：`Log：仅一行「no-act，快速通道输出」`。
- S2g. P2：S3-MSG7 登场门缺 spawn+终局行——已在 G8 覆盖。
- S2h. 分支A/B 多黄毛门控——同 S1f 改法（FT L347/L348）。

### S3. DEI（纯雄竞）
- S3a. P0：S2-MSG4 胜负确认轮终局不可达（DEI L457 追踪「不再列出」vs L493/L535/L537）——G2（追踪仍列出）+ G5（确认轮例外）+ G1（终局不踢出）组合覆盖；另需确认胜负确认轮该黄毛进入行动名单：在 DEI L548（行动名单行）`本轮新 spawn + 已真正锁定/活跃黄毛` 后补 `（含胜负确认轮刚确认终局/败·友好的黄毛——见胜负确认轮例外）`（按 dump 精确 OLD 插入）。
- S3b. P1：S3-MSG0 L639 no_spawn=本轮无黄毛 与 L745 追踪黄毛登场矛盾——G14（S3-MSG0 刷新状态行）+ G9（登场门 no_spawn 行）组合覆盖。
- S3c. P1：S3-MSG0 L745「照常编排」vs 场景外 L655/L922——G9 覆盖（no_spawn 行改写后不再有「照常编排」字面）。
- S3d. P1：S2-MSG4 L376 vs L356 离场黄毛处置矛盾（一处 no_spawn「再无行动可能」似闭合、一处 no-act 持续追踪）——G3c（no_spawn ② 行改为「黄毛不在当前场景仍可判 act；线闭合仅黄毛败·友好/彻底离场」）覆盖；另把 L376 附近「黄毛远走他乡且无尾随目标途径」后的线状态补 `（若为暂时无途径则 no-act 但持续追踪，线不死寂）`（按 dump 精确位置插入）。
- S3e. P2：`明确且长期拒绝`（DEI L493/666/681/1055/1185）——G10 覆盖。
- S3f. P2：S2 世界书选择保留「NTR/绿帽」类别（DEI L264）——纯爱版移除「绿帽」：`NTR/绿帽/竞争/信息差旁观` → `NTR/竞争/信息差旁观`。
- S3g. P2：章节目标「进度」列 ○/◐/●/✗ + 「任意两目标达成即切换」（DEI L941-946）——通用模板进度机制残留：把 `○/◐/●/✗` 进度列说明改为剧情语言（`达成/进行中/未开始`），「任意两个目标达成或失败/无法完成→切换」改为 `任意两个目标的章节目标达成交付 → 切换`（按 dump 精确 OLD 替换）。
- S3h. P2：S3-MSG15「体态」vs「型体概要」（DEI L1103）——`体态` 统一为 `型体概要`（该处若为字段名混用，按 dump 精确替换）。
- S3i. 分支A/B 多黄毛门控——同 S1f 改法（DEI L356/L357）。

### S4. straight_NTRS
- S4a. P0-3：S3-MSG2 L669「胜负均由 stage 2 判定…不重新判定」vs S3-MSG17 sparkNotes L1223 自主判定——把 sparkNotes 处 `雄竞期·胜负判定…对象是否明确选择黄毛→黄毛胜…明确且长期拒绝→黄毛败` 改为核对式：`雄竞期·胜负核对（以 stage 2 thugSpawn 线状态为准，仅核对剧情事件是否一致）：thugSpawn 线状态=黄毛胜·终局？=NTRS期/黄毛败·友好？未分胜负则雄竞期——禁止 S3 自主判定胜负`（按 dump 精确 OLD 替换，G10 同时处理「明确且长期拒绝」措辞）。
- S4b. P1-1：场景外 vs prologue 名单泄漏——G9 覆盖（no_spawn 行改写）。
- S4c. P1-2：prologue 机缘/暗流节输出门互斥（L763 必发 vs L837-840 仅知情时输出）——补一句：在 L763（thugAction=act → 该节必发）处追加 `（场景外 act 且 {{user}} 完全不知🌙 → 该节不输出，仅 stage 记录）`。
- S4d. P2-2：S2-MSG4 L375「若无活跃追踪黄毛则下游 stage3 走快速通道」——G3c 覆盖（NEW 已改为「行动判定为 no-act 时下游 stage3 走快速通道」）。
- S4e. P2-3：L375「黄毛远走他乡且无尾随途径→线闭合」vs L284/355「已无接近途径→no-act 但持续追踪」矛盾——G3c 覆盖（NEW 已统一「线闭合仅黄毛败·友好/彻底离场；暂时无途径则 no-act 持续追踪」）。
- S4f. P2-4：S2-MSG4 L436「`<thugSpawn>` 标签内**只放**刷新状态+黄毛人设」漏提追踪区块——改为 `**只放**刷新状态+黄毛人设+【黄毛动向追踪】区块`。
- S4g. P2-5：S3-MSG2 门槛表/五阶段仍列忠诚/动摇型行（L715-721/L704-709）——虽注明「不出现」，改为在表头标注 `（本版 41% 起步，忠诚/动摇型不出现）`（按 dump 精确 OLD 在「五阶段」「门槛表」标题处各补标注；若已有「不出现」标注则跳过）。
- S4h. P2-6：重复/残句（S2-MSG2 L328-331 已由 G15a 覆盖；S3-MSG15 L1128-1129 已由 G15b 覆盖；S3-MSG2 L665 残句「黄毛的互动积累，已察觉…」按 dump 上下文修复为完整句；S2-MSG4 L554「标签紧接在</sparkNotes>后（thugSpawn→…）」表述改为 `<thugSpawn> 标签紧接在 </sparkNotes> 后`；stage 模板 L903「NTR 标记列」由 G11 覆盖）。
- S4i. G15 表格权威源降级（S4 版专属，见 G15）。

### S5. FT_NTRS
- S5a. P0-1：S2-MSG0 vs S2-MSG2 表格互斥——G15a 覆盖。
- S5b. P0-2：S3-MSG0 vs S3-MSG15 表格互斥 + 重复句——G15b 覆盖。
- S5c. P0-3：S2-MSG4 L379 no_spawn 定义焊死——G3a/G3b 覆盖。
- S5d. P1-1：S2-MSG4 L381 分支B 子情形横跳（「所有💔角色均已绑定黄毛」实属分支A）——把 `① 分支B——无未绑定黄毛的💔目标（无💔角色 / 所有💔角色均已绑定黄毛）` 改为 `① 分支B——无未绑定黄毛的💔目标（无💔角色）；所有💔角色均已绑定黄毛 → 归分支A 按追踪判定`。
- S5e. P1-2：S2-MSG4 L532 胜负确认轮强判 act vs 硬约束门——在例外句（G5 的 NTRS 版）补 `（若逻辑无硬伤门严重不成立——黄毛与对象天各一方且无任何近身契机——则不例外，仍判 no-act 待更合理时机）`。
- S5f. P2-1：S2-MSG4 L561「标签紧接在</sparkNotes>后」→ `<thugSpawn> 标签紧接在 </sparkNotes> 后`。
- S5g. P2-2：S3-MSG2 L674「推波助澜从「半明示」层级起步（察觉型前后）」→ 删括号：`推波助澜从「半明示」层级起步`。
- S5h. P2-3：型体概要（追踪区块/下游直读）vs 型体设定（表格列名）——在 S2-MSG2 表格段（G15a NEW 中已用「型体设定」）补注 `（型体设定=追踪区块中的型体概要，同义）`；或在追踪格式行（L445/449）补 `（型体概要=表格中的型体设定）`（按 dump 就近补一处即可）。

### S6. DEI_NTRS
- S6a. P0：S2 表格权威互斥——G15a 覆盖。
- S6b. P1-1：S2-MSG4 TRIGGER 0 末尾「胜负确认后的后续轮才回归 no-act」vs TRIGGER 1 NTRS期恢复判定冲突（DEI_NTRS L543 vs TRIGGER 1）——G5（NTRS 版例外句改为「黄毛败转 NTRS期后续轮按 NTRS 期判定、黄毛胜后续轮不回归 no-act」）覆盖。
- S6c. P1-2：追踪列出矛盾（L469 不再列出 vs L505 反映胜负后线状态）——G2 覆盖（终局仍列出）。
- S6d. P2-1：`明确且长期拒绝`（L505/1251）——G10 覆盖。
- S6e. P2-2：S3-MSG2 L787 no_spawn 照常编排 vs 场景外——G9 覆盖。
- S6f. P2-3：S3-MSG2 L783-786 登场门自缠绕（标注「竞争者·[五型]·雄竞期」又要求 prologue 不得出现系统术语）——在登场名单标注句后补 `（名单标注为内部调度，prologue 正文以剧情语言表达）`（按 dump 精确位置插入；若已有类似说明则跳过）。
- S6g. P2-4：型体设定/型体概要混用（L343 vs L460/469/645/1169）——同 S5h 就近补注一处。
- S6h. P2-5：S3-MSG15 L1155-1156 重复句——G15b 覆盖。
- S6i. P2-6：S3-MSG0 快速通道恢复「spawn 或 act」——G12 覆盖。
