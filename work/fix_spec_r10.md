# R10 修复规格 —— 亲密开局空白补全（纯雄竞 ABC + NTRS·雄竞亲密开局分流，BATTLE 六版）

> 执行方式：对 `C:\Users\zouyu\Downloads\酒馆\数据库\剧情推进预设\` 下 BATTLE 六版（`Cirno_BATTLE_Turn_straight.json` / `_FT.json` / `_DEI.json` / `_straight_NTRS.json` / `_FT_NTRS.json` / `_DEI_NTRS.json`）做插入式修改。OLD 锚点给出「唯一特征串」，执行者须先 Read 文件确认锚点在当前文件中存在（R9 后状态），再在锚点之后/之内插入 NEW 文本。替换/插入后 JSON.parse 成功、顶层仍是数组（首字符 [ ）、引号与 \n 转义正确（content 字段内的换行写为字面 \n）。备份已由主代理完成（.bak-pre-r10），执行者不再备份。写入须持有对象引用（obj[key] 原地改），不可对字符串副本操作。**只改 BATTLE 六版，不动原 NTRS 12 版。**

## 全局语义基线（先读）
- **纯雄竞三版**（straight/FT/DEI）：无 NTRS 出口——亲密开局（对象出场即恋人/配偶/已站队）目标**留在雄竞期**，用 A（败判据暂缓）+ B（守成方编排）+ C（胜需积累）补齐空白。
- **NTRS·雄竞三版**（straight_NTRS/FT_NTRS/DEI_NTRS）：亲密开局目标**直接分流到 NTRS 线**——不经过雄竞期；对象接受程度从低接受度完整五阶段（忠诚/动摇/察觉/默契/乐享）起步、推波助澜从暗中起步；雄竞期（自由身目标）保持现状不变；雄竞 user 赢来的对象维持本版 NTRS期 41% 察觉型起步不变。
- NTRS推进 user 设定（用户 2026-08-02 澄清）：**user 自始至终有 NTRS 癖好，对象进度条=接受程度**。
- 脚手架同步原则（R9 准则）：判定语义改动必须同步 S2 desc（description 字段）+ 正文 HARD RULES + S3 编排 + S3 desc + 注意力自检。
- 保密原则：给填表 AI/下游的调度词不进正文（prologue/stage 禁系统术语既有规则不变）。

---

## PART A：纯雄竞三版（straight / FT / DEI）

### A1. S2-MSG0 判定主规则：新增「已站队对象胜负判定」条款（判定层 A+C）
**插入位置**：S2-MSG0（内容含「**对象情感倾向影响雄竞难度**」的那条 bullet）**之后**，紧接插入新 bullet。
**OLD 锚点**（三版同）：
`- **对象情感倾向影响雄竞难度**：对象对 {{user}} 有明显情感倾向（已是恋人/配偶、或 {{user}} 深爱且对象已察觉/有回应）→ 黄毛竞争难度高，黄毛需更多行动积累才可能赢得对象；对象对 {{user}} 无情感倾向或处于游离状态 → 黄毛竞争相对容易。`
**NEW**（在锚点后追加一条 bullet）：
`- **已站队对象胜负判定（亲密开局专用）**：对象出场即与 {{user}} 已是恋人/配偶/已明确站队 {{user}} 时——① **黄毛败判据暂缓**：雄竞期内对象对 {{user}} 的既有态度/行为/话语倾向（既定关系的常态）**不计入黄毛败判据**——黄毛败仅在黄毛已取得实质进展（对象出现动摇事件：态度松动/暧昧回应/私下往来）之后、对象仍明确选择 {{user}}（拒绝黄毛邀约/疏远/表态）时才可判；② **黄毛胜须积累**：已站队对象不因单轮行动判黄毛胜——黄毛胜须经多轮实质进展积累（对象动摇事件逐轮累积）后，对象明确离弃 {{user}}、选择黄毛的剧情事件（表态/确立关系/成婚）确认；③ 未达成以上判据前，该对象线维持雄竞期、黄毛持续行动判定照常。`
**注意**：straight 版该 bullet 之后是「- 判断只看当下剧情与时空合理性…」；FT/DEI 版同样。插入后保持 bullet 序列连续。

### A2. S2 description：同步「已站队对象胜负判定」（判定层 A+C 的 desc）
**插入位置**：S2 desc（"description" 字段，内容含「黄毛败=综合判断女主行为已选择{{user}}（对两人的态度/行为/话语倾向{{user}}）」的那句）**之后**追加。
**OLD 锚点**（straight 版）：
`黄毛败=综合判断女主行为已选择{{user}}（对两人的态度/行为/话语倾向{{user}}）（对象与黄毛变好朋友、黄毛线闭合，{{user}}与对象纯爱恋爱）`
（FT/DEI 版该句尾还紧跟「**男娘系黄毛（…）败后触发天意·后宫线…**」——锚点取「黄毛败=综合判断女主行为已选择{{user}}（对两人的态度/行为/话语倾向{{user}}）」部分，FT/DEI 只在此句后追加）
**NEW**（追加）：
` 已站队对象（出场即恋人/配偶/已明确站队）雄竞期内既有倾向不计黄毛败，黄毛败须黄毛实质进展后对象仍明确选择{{user}}才可判；黄毛胜须多轮进展积累+对象明确离弃选择黄毛的剧情事件确认`

### A3. S3-MSG2「雄竞期编排」段：新增「已站队对象（亲密开局）编排」子段（编排层 B + C 编排侧）
**插入位置**：S3-MSG2（内容含「**【雄竞期编排（线状态=雄竞期）】**」）段内，「- 雄竞期黄毛可以真正赢得对象的心——黄毛胜即该对象线终局锁定。」这一行**之后**，追加子段。
**OLD 锚点**（三版同）：
`- 雄竞期黄毛可以真正赢得对象的心——黄毛胜即该对象线终局锁定。`
**NEW**（在锚点后追加）：
`- **已站队对象（亲密开局）编排**：对象与 {{user}} 已是恋人/配偶/已明确站队时——{{user}} 是守成方而非追求方：竞争反应为守护/不安/查岗/紧张吃醋/暗自提防（不写「追求加码」语言）；对象在既定关系内的动摇须合理化（相处倦怠/新鲜感缺失/诱惑冲击/信任裂缝等关系内危机驱动，非突兀劈腿），动摇幅度随黄毛行动积累逐轮加深；黄毛竞争难度高，须多轮实质行动积累才可能撬动对象——黄毛胜的终局戏为对象明确离弃 {{user}} 改选黄毛（表态/确立关系/成婚），与该积累过程衔接，禁止单轮撬动判胜。`
**注意**：FT/DEI 版该行后是「**【黄毛败·友好 + 天意·后宫线编排…】**」段——子段插在两者之间。

### A4. S3 description：同步「亲密开局编排」（编排层 B）
**插入位置**：S3 desc（"description" 字段，内容含「按线状态（雄竞期/黄毛败·友好/黄毛胜·终局）编排」的那段）——在「雄竞期落实 {{user}} 正常追求与竞争张力」之后插入「亲密开局」语义。
**OLD 锚点**（三版同）：
`雄竞期落实 {{user}} 正常追求与竞争张力（黄毛与{{user}}争夺可攻略对象，胜负靠剧情无数值）`
**NEW**（整句替换）：
`雄竞期落实 {{user}} 正常追求与竞争张力（黄毛与{{user}}争夺可攻略对象，胜负靠剧情无数值；对象已站队/亲密开局时 {{user}} 为守成方——守护/不安/查岗/吃醋提防，对象动摇合理化，黄毛须多轮积累才可能撬动）`
（FT/DEI 版该句后是「，黄毛败·友好落实对象与黄毛变好朋友…」，直接替换前缀即可）

### A5. S3 注意力自检：新增「亲密开局」核验题（脚手架同步）
**插入位置**：S3 注意力自检八题（内容含「**五、竞争张力核验**」的那条）**之后**追加一条。
**OLD 锚点**（三版同）：
`5. **竞争张力核验**：本轮是否体现 {{user}} 与黄毛之间的竞争张力？{{user}} 的追求/竞争反应是否真实有效？`
**NEW**（在锚点后追加）：
`6. **亲密开局核验**：若本轮涉及已站队对象（与 {{user}} 已是恋人/配偶），是否按守成方编排（{{user}} 守护/不安/查岗而非追求加码、对象动摇合理化、黄毛未凭单轮行动被判胜）？（原 6-8 题顺延编号为 7-9）`

---

## PART B：NTRS·雄竞三版（straight_NTRS / FT_NTRS / DEI_NTRS）

### B1. S2-MSG0 判定主规则：新增「亲密开局分流」条款（判定层核心）
**插入位置**：S2-MSG0（内容含「**对象已站队不豁免**」那条 bullet）**之后**，紧接插入新 bullet。
**OLD 锚点**（三版同）：
`- **对象已站队不豁免**：对象已是 {{user}} 的恋人/配偶/已明确站队 {{user}} 时，黄毛刷新、行动、竞争成功的判定照常执行，不降级、不豁免——雄竞可能拆散既定关系。`
**NEW**（整句替换 + 追加新 bullet）：
`- **对象已站队→亲密开局分流（替代雄竞竞争）**：对象已是 {{user}} 的恋人/配偶/已明确站队 {{user}} 时，该目标**不进入雄竞期**——刷新黄毛锁定后线状态直接=NTRS期·亲密开局，黄毛积极行动门（对象与 {{user}} 亲密即激活）满足即 act，按 NTRS推进逻辑推进（见 B2）。`
`- **亲密开局 NTRS 线起点**：NTRS期·亲密开局的对象接受程度从低接受度完整五阶段（忠诚/动摇/察觉/默契/乐享）起步（区别于黄毛败转 NTRS期的 41% 察觉型起步）；推波助澜从暗中起步；📹 事后知情从察觉型起（忠诚/动摇期一律 🌙）。`
**注意**：原「对象已站队不豁免」条款被替换——纯雄竞版仍保留该条款（见 A1），NTRS·雄竞版改走分流。

### B2. S2 task_rules 分支B：刷新判定加「关系状态分流」
**插入位置**：S2 task_rules 的「【分支 B — 有待刷新目标】」行**之后**追加一句。
**OLD 锚点**（三版同）：
`【分支 B — 有待刷新目标】：场上存在**尚未绑定黄毛**的💔可攻略目标（无论场上是否已有其他黄毛在追踪），对其走"黄毛刷新判定"逻辑判定本轮是否为该目标刷新一个新黄毛；已有追踪黄毛的目标走分支A 追踪写法。`
**NEW**（在锚点后追加）：
`【分支 B-亲密开局分流】：对有待刷新目标先判其与 {{user}} 的关系状态——目标出场即与 {{user}} 已建立亲密关系（恋人/配偶/已明确站队）→ 走亲密开局分流：刷新黄毛锁定后线状态直接=NTRS期·亲密开局（接受程度从低接受度五阶段起步），不经过雄竞期，黄毛积极行动门满足即 act；目标未与 {{user}} 亲密 → 照常进入雄竞期。`

### B3. S2 description：同步「亲密开局分流」（desc）
**插入位置**：S2 desc（"description" 字段，内容含「黄毛出手不依赖 {{user}}-对象亲密关系（对象已站队也不豁免）」的那句）——替换该句为含分流的表述。
**OLD 锚点**（三版同）：
`黄毛出手不依赖 {{user}}-对象亲密关系（对象已站队也不豁免）：只要可攻略角色出现+刷新合理+行动合理即出手与 {{user}} 竞争`
**NEW**（整句替换）：
`黄毛出手不依赖 {{user}}-对象亲密关系：自由身目标只要可攻略角色出现+刷新合理+行动合理即出手与 {{user}} 竞争（雄竞期）；对象已站队（出场即恋人/配偶/已明确站队）→ 亲密开局分流——刷新锁定后直接 NTRS期·亲密开局（接受程度从低接受度五阶段起步、黄毛积极行动门满足即 act），不经过雄竞期`

### B4. S3-MSG2 双期状态机「NTRS期」定义 + 「NTRS期编排」段：新增亲密开局路径（编排层）
**插入位置 a**：S3-MSG2 状态机「**NTRS期**」定义 bullet（内容含「原 NTRS 核心逻辑全面适用（推波助澜、淫妻线五阶段、身体接受度门槛表、知情度三档、黄毛真情约束）」）**之后**追加一句。
**OLD 锚点 a**（三版同）：
`- **NTRS期**：黄毛败（综合判断女主行为已选择 {{user}}——对两人的态度/行为/话语倾向 {{user}}）——{{user}} 赢得对象，**NTRS癖好从隐秘转为显性**——user不再隐藏XP，推波助澜从半明示起步；原 NTRS 核心逻辑全面适用（推波助澜、淫妻线五阶段、身体接受度门槛表、知情度三档、黄毛真情约束）。`
**NEW a**（在锚点后追加）：
`- **NTRS期·亲密开局（对象出场即与 {{user}} 亲密）**：不走雄竞期，直接进入 NTRS 线——对象接受程度从低接受度完整五阶段（忠诚/动摇/察觉/默契/乐享）起步、推波助澜从暗中起步，随接受程度逐阶段演进；对象察觉后迎合（察觉型起），口述报告/视频=兴奋源（📹 事后知情从察觉型起，忠诚/动摇期一律 🌙）；区别于黄毛败转 NTRS期（41% 察觉型起步）。`
**插入位置 b**：S3-MSG2「**【NTRS期编排（线状态=NTRS期，黄毛败后激活）】**」段开头（标题行之后）追加一段「亲密开局」编排说明。
**OLD 锚点 b**（三版同）：
`**【NTRS期编排（线状态=NTRS期，黄毛败后激活）】**`
**NEW b**（在锚点后追加）：
`**【NTRS期·亲密开局编排（线状态=NTRS期·亲密开局）】**对象出场即与 {{user}} 已是恋人/配偶/已站队——本路径跳过雄竞期直接进入 NTRS 线：对象接受程度五阶段从忠诚/动摇起步，黄毛行动=对亲密对象的暧昧/渗透（按黄毛五型手段），user 有 NTRS 癖好、推波助澜从暗中起步（暗中安排机会/创造独处/制造巧合，对象未察觉），接受程度升入察觉型后对象察觉迎合、推波转半明示（放行/默契），口述报告/录像=兴奋源（📹 事后知情从察觉型起）；接受程度进度按触发事件分量 +0~5%/轮推进（同 NTRS期规则）。`

### B5. S3 description：同步「亲密开局」（desc）
**插入位置**：S3 desc（"description" 字段，内容含「按线状态（雄竞期/NTRS期/黄毛胜·终局）编排」）——在「NTRS期落实知情度三档（在场见证/事后知情/完全不知）与淫妻线进度」之后插入亲密开局语义。
**OLD 锚点**（三版同）：
`NTRS期落实知情度三档（在场见证/事后知情/完全不知）与淫妻线进度`
**NEW**（整句替换）：
`NTRS期落实知情度三档（在场见证/事后知情/完全不知）与淫妻线进度（含亲密开局路径：对象出场即与{{user}}亲密→直接 NTRS期·亲密开局，接受程度从低接受度五阶段起步、推波从暗中起步、察觉型起📹事后知情）`

### B6. S3 注意力自检：进度核验题兼容亲密开局（脚手架同步）
**插入位置**：S3 注意力自检「**3. 进度一致（仅 NTRS期适用）**」那条——追加亲密开局说明。
**OLD 锚点**（三版同）：
`3. **进度一致（仅 NTRS期适用）**：stage 里每个 X% / +X% 是否都能在 sparkNotes「NTRS 进度结算」找到同一数字？sparkNotes 未写清结算 → 先补思考再写 content。快速通道场景下整段自检跳过、本条不适用；act 档下 NTRS期 act 幅度是否落在 +0~5%？雄竞期无数值进度，此项不适用。`
**NEW**（整句替换）：
`3. **进度一致（仅 NTRS期适用，含亲密开局）**：stage 里每个 X% / +X% 是否都能在 sparkNotes「NTRS 进度结算」找到同一数字？sparkNotes 未写清结算 → 先补思考再写 content。快速通道场景下整段自检跳过、本条不适用；act 档下 NTRS期 act 幅度是否落在 +0~5%？雄竞期无数值进度，此项不适用；NTRS期·亲密开局进度同 NTRS期规则（低接受度起步、+0~5%/轮）。`

---

## 验证清单（执行者完成后自查）
1. JSON.parse 成功、顶层首字符 = `[`。
2. A1/A3/A5 锚点均在纯雄竞三版生效（FT/DEI 需确认锚点在男娘系版本中同样存在；若有差异按实际文本微调后记录）。
3. B1/B2/B4 锚点均在 NTRS·雄竞三版生效。
4. 替换后不残留「对象已站队不豁免」在 NTRS·雄竞版（被 B1 替换）；纯雄竞版「对象已站队不豁免」保留。
5. 报告每版实际插入/替换点数量与位置（msg 序号 + 字段名），未命中锚点须报告原始文本。
