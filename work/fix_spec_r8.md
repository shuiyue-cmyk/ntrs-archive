# R8 修复规格 —— 六版 BATTLE 预设（spawn 基准收紧 + 场景外戏正文呈现 + 同场主动制造机会 act）

> 执行方式：对 `C:\Users\zouyu\Downloads\酒馆\数据库\剧情推进预设\Cirno_BATTLE_Turn_*.json` 做精确字符串替换。
> 参考可读文本：`C:\Users\zouyu\Downloads\BATTLE_work\review_dump\Cirno_BATTLE_Turn_*.txt`（修改前 dump，OLD 以本规格为准——本规格 OLD 已按 R7 修改后的实际文本核对）。
> 替换发生在 JSON 中 plotTask 各消息 content 字符串。OLD 必须精确匹配（含全角/半角、emoji、{{user}}、<tag>），先 grep 确认存在再替换。替换后 JSON.parse 必须成功、顶层必须仍是数组（首字符 [ ）。备份已由主代理完成（.bak-pre-r8），执行者不再备份。

## 背景（用户 2026-08-06 三项决策）

1. **spawn 基准收紧**：黄毛**本轮能进入 {{user}} 当前场景画面**（当场出现/本轮内有合理进入画面路径）才 spawn；同楼其他房间/隔壁/附近区域/远房等**不在当前场景画面内** → no_spawn（即使后续轮次可能有出场机会，也不算 spawn）。
2. **场景外戏正文呈现**：黄毛与配对对象均在 {{user}} 当前场景之外互动时（no_spawn + act），**正文 content 也完整编排该场景外戏**（读者可见全貌，{{user}} 角色不知情），stage 标注「场景外场景」，prologue 不把该戏作为 {{user}} 在场戏展开。
3. **同场主动制造机会 act**：对象与 {{user}} 同处当前场景时，黄毛虽不在当前场景画面内（no_spawn），可主动制造对象离场契机（约定/诱引/外部事件等合理手段）后，在 {{user}} 场景外对对象展开行动（照常判 act）。

## 改动组 R8-A：spawn 基准收紧（六版 S2-MSG4）

### A1. HARD RULES 3「出场可能性判定」整段
- OLD：
```
3. **出场可能性判定（刷新成功标准，替代纯时空合理性）**：
 - 必须从角色卡、世界书、背景设定、当前剧情线、后续场景趋势中查证：**接下来的场景中该黄毛是否有实际出现的可能**（有出场契机/进入画面的路径/与目标互动的机会）？
 - 若黄毛只是"存在"但当前与后续场景都没有出场与互动的可能（如同楼住户、远房路人，{{user}} 与对象在家私密互动时黄毛在自己家毫无关联）→ 判 **no_spawn**（不空刷新）
 - 若接下来的场景中黄毛有合理出场路径（目标将去公共场所、黄毛可被引荐/偶遇/主动接近、后续互动有展开空间）→ 判 **spawn**
```
- NEW：
```
3. **出场可能性判定（刷新成功标准，替代纯时空合理性）**：以 **{{user}} 本轮当前场景画面** 为唯一基准——黄毛**本轮能否进入 {{user}} 当前场景画面**（本轮当场出现 / 本轮内有合理进入画面的路径与目标互动）→ 判 **spawn**；**黄毛不在 {{user}} 当前场景画面内**（同楼其他房间、隔壁、附近区域、远房等——即使后续轮次可能有出场机会）→ 一律判 **no_spawn**（不空刷新）。黄毛与配对对象在本轮 {{user}} 场景之外互动 = 场景外行动（no_spawn 仍可判 act，见行动判定段），与 spawn 判定无关。
```

### A2. 分支B spawn ①「刷新成功判定标准」句
- OLD：`**刷新成功判定标准 = 接下来的场景中是否会有黄毛出现的可能**（后续剧情是否有黄毛实际出场的契机/进入画面的路径/与目标互动的机会）——**若黄毛仅是"存在"（如同楼住户/远房路人）但当前及后续场景都没有其出场与互动的可能 → 判 no_spawn，不空刷新**`
- NEW：`**刷新成功判定标准 = 本轮黄毛能否进入 {{user}} 当前场景画面**（本轮当场出现 / 本轮内有合理进入画面的路径）——**黄毛不在 {{user}} 当前场景画面内（同楼其他房间/隔壁/附近/远房等，即使后续可能有出场机会）→ 判 no_spawn，不空刷新**`

### A3. 分支A spawn ②「spawn=」定义句
- OLD：`**spawn=本轮黄毛在当前场景在场（或本轮新刷新登场）；黄毛不在当前场景（含离场追踪/场景外行动）=no_spawn**`
- NEW：`**spawn=本轮黄毛在 {{user}} 当前场景画面内在场（或本轮新刷新进入画面）；黄毛不在 {{user}} 当前场景画面内（含同楼其他房间/隔壁/离场追踪/场景外行动）=no_spawn**`

### A4. no_spawn 头注
- OLD：`- **no_spawn**：本轮无黄毛在当前场景在场（追踪中/离场黄毛仍可能行动）。两种情形：`
- NEW：`- **no_spawn**：本轮无黄毛在 {{user}} 当前场景画面内（同楼其他房间/隔壁/离场追踪/场景外行动——追踪中/离场黄毛仍可能行动）。两种情形：`

### A5. no_spawn ② 分支A 行（开头补「不在画面内」）
- OLD（straight/FT/DEI）：`② 分支A——已有追踪黄毛：黄毛不在当前场景（离场追踪/场景外行动）——若黄毛与对象均不在 {{user}} 当前场景但两者可接触（黄毛离场前往对象所在处攻略），仍可判 act（场景外行动，发生在 {{user}} 场景外）；若黄毛线已闭合（黄毛败·友好）或黄毛彻底离场再无行动可能（如黄毛远走他乡且无尾随目标途径）→ no-act。行动判定为 no-act 时下游 stage3 走快速通道。`
- OLD（NTRS 三版，闭合枚举为「黄毛败·友好」或按各版实际）：按各版 dump 精确取（结构与 straight 相同，仅闭合枚举措辞可能不同，如「黄毛败·友好」）。
- NEW（六版通用）：`② 分支A——已有追踪黄毛：黄毛不在 {{user}} 当前场景画面内（同楼其他房间/隔壁/离场追踪/场景外行动）——若黄毛与对象均不在 {{user}} 当前场景画面但两者可接触（黄毛离场前往对象所在处攻略，或黄毛制造对象离场契机后场景外接触），仍可判 act（场景外行动，发生在 {{user}} 场景外）；若黄毛线已闭合（黄毛败·友好）或黄毛彻底离场再无行动可能（如黄毛远走他乡且无尾随目标途径）→ no-act。行动判定为 no-act 时下游 stage3 走快速通道。`

### A6.（可选）分支A 追踪格式示例行补注
- OLD（straight 等六版）：`- [黄毛名]（锁定目标 [对象名]）：动向=[在场·[位置] / 离场·[去向/尾随目标/潜伏接近] / 暗中布局]；线状态=[雄竞期/黄毛败·友好/黄毛胜·终局]；五型=[权力型/魅力型/隐秘型/强制型/诱惑型]；型体概要=[1句，如"外表温和清秀，性器官勃起时足够粗长持久"]`
- NEW（六版通用，NTRS 版线状态枚举按各版实际替换）：
  `- [黄毛名]（锁定目标 [对象名]）：动向=[在场·[位置]（当前场景画面内） / 离场·[同楼其他房间/去向/尾随目标/潜伏接近] / 暗中布局]；线状态=[雄竞期/黄毛败·友好/黄毛胜·终局]；五型=[权力型/魅力型/隐秘型/强制型/诱惑型]；型体概要=[1句，如"外表温和清秀，性器官勃起时足够粗长持久"]`
  （NTRS 版线状态枚举为 [雄竞期/NTRS期/黄毛胜·终局]，FT/DEI 含 [后宫线]——按各版实际枚举替换 OLD 中段即可。）

## 改动组 R8-B：场景外戏正文呈现（六版 S3）

### B1. S3-MSG0 act 编排「场景外」句
- OLD（straight，含 R7 补注）：`**若黄毛与对象均在 {{user}} 当前场景之外、但两者可接触（黄毛离场前往对象所在处攻略），本轮黄毛行动发生在 {{user}} 场景外——stage 须标注「场景外场景」，prologue 不展开该场景外戏**（场景外行动 {{user}} 必不在场，{{user}} 知情度不得为👁️，只能 📹 事后知情或 🌙 完全不知）`
- OLD（FT/DEI/NTRS 三版，无 R7 补注）：`**若黄毛与对象均在 {{user}} 当前场景之外、但两者可接触（黄毛离场前往对象所在处攻略），本轮黄毛行动发生在 {{user}} 场景外——stage 须标注「场景外场景」，prologue 不展开该场景外戏**`
- NEW（六版通用）：`**若黄毛与对象均在 {{user}} 当前场景之外、但两者可接触（黄毛离场前往对象所在处攻略，或黄毛制造对象离场契机后场景外接触），本轮黄毛行动发生在 {{user}} 场景外——stage 须标注「场景外场景」，正文 content 完整编排该场景外戏（读者可见黄毛与对象的互动全貌，{{user}} 角色不知情，属 📹 事后知情或 🌙 完全不知的暗线戏），prologue 不把该戏作为 {{user}} 在场戏展开**（场景外行动 {{user}} 必不在场，{{user}} 知情度不得为👁️，只能 📹 事后知情或 🌙 完全不知）`

### B2. S3 stage 模板「场景外标注」字段
- OLD（straight，含 R7 补注）：`- **场景外标注:** 仅当本轮黄毛与对象均在 {{user}} 当前场景之外、黄毛离场前往对象所在处攻略时填「场景外场景」——该戏发生在 {{user}} 视线外，{{user}} 不知情，stage 记录、prologue 不展开（场景外行动 {{user}} 必不在场，{{user}} 知情度不得为👁️，只能 📹 事后知情或 🌙 完全不知）`
- OLD（FT/DEI/NTRS 三版，无 R7 补注）：`- **场景外标注:** 仅当本轮黄毛与对象均在 {{user}} 当前场景之外、黄毛离场前往对象所在处攻略时填「场景外场景」——该戏发生在 {{user}} 视线外，{{user}} 不知情，stage 记录、prologue 不展开`
- NEW（六版通用）：`- **场景外标注:** 仅当本轮黄毛与对象均在 {{user}} 当前场景之外、黄毛离场前往对象所在处攻略（或制造对象离场契机后场景外接触）时填「场景外场景」——该戏发生在 {{user}} 视线外，{{user}} 不知情，**stage 记录 + 正文 content 完整编排该场景外戏（读者可见全貌）**（场景外行动 {{user}} 必不在场，{{user}} 知情度不得为👁️，只能 📹 事后知情或 🌙 完全不知）`

### B3. S3-MSG2 登场门 no_spawn 行（R7 已改写版）
- OLD（六版，R7 后文本）：` - thugSpawn 状态=no_spawn → 本轮无新黄毛登场；prologue 登场名单不列入新黄毛；追踪中的活跃黄毛若本轮 act 且行动发生在 {{user}} 当前场景内，按对应线状态规则登场编排；若黄毛与对象均在 {{user}} 当前场景之外（场景外行动），该戏仅写入 stage（标注「场景外场景」），prologue 不展开、黄毛不列入登场名单。`
- NEW（六版通用）：` - thugSpawn 状态=no_spawn → 本轮无新黄毛登场；prologue 登场名单不列入新黄毛；追踪中的活跃黄毛若本轮 act 且行动发生在 {{user}} 当前场景内，按对应线状态规则登场编排；若黄毛与对象均在 {{user}} 当前场景之外（场景外行动），该戏写入 stage（标注「场景外场景」）+ 正文 content 编排（读者可见，{{user}} 角色不知情），prologue 不展开、黄毛不列入登场名单。`

## 改动组 R8-C：同场主动制造机会 act（六版 S2）

### C1. S2 行动判定 act 段「目标离场时黄毛仍可 act」句
- OLD（六版）：`**目标离场时黄毛仍可 act**：黄毛可尾随、赶赴、潜伏接近目标所在处展开行动（只要逻辑无硬伤门全过）。`
- NEW（六版通用）：`**目标离场时黄毛仍可 act**：黄毛可尾随、赶赴、潜伏接近目标所在处展开行动（只要逻辑无硬伤门全过）。**目标与 {{user}} 同处当前场景时黄毛仍可 act**：黄毛虽不在 {{user}} 当前场景画面内（no_spawn），但可主动制造目标离开 {{user}} 场景的机会（约定/诱引/传递信息/外部事件引走目标等合理手段）后，在 {{user}} 场景外对目标展开行动——场景外行动照常判 act（见 no_spawn ②）。`

### C2. 硬约束「逻辑无硬伤门」近身契机行
- OLD（六版）：` - 近身契机：黄毛本轮与目标在同一空间，或有合理理由在本轮内自然抵达（**目标离场不算禁行**——黄毛可尾随/赶赴/潜伏接近目标所在处，只要剧情上途径与动机合理）`
- NEW（六版通用）：` - 近身契机：黄毛本轮与目标在同一空间，或有合理理由在本轮内自然抵达（**目标离场不算禁行**——黄毛可尾随/赶赴/潜伏接近目标所在处；**目标与 {{user}} 同处当前场景时**，黄毛有合理手段制造目标离场契机（约定/诱引/外部事件引走）后场景外接触，途径与动机合理即可，只要剧情不荒谬）`

## 版本差异注意
- NTRS 三版（straight_NTRS / FT_NTRS / DEI_NTRS）与纯雄竞三版（straight / FT / DEI）在上述各组的 OLD 大多相同；差异仅在：
  - A5 闭合枚举（NTRS 版为「黄毛败·友好」或按 dump 实际）
  - A6 线状态枚举（NTRS 版含 NTRS期、FT/DEI 含后宫线）
  - B1/B2 是否含 R7 补注「（场景外行动 {{user}} 必不在场…）」——含则按带补注 OLD，不含则按不带补注 OLD，NEW 统一为带补注版本
- 逐对替换时先 grep 文件确认 OLD 存在；0 命中项报告原因（可能措辞与规格略有差异，从 dump 取实际文本后仍按规格 NEW 语义替换）。

## 验证要求
1. JSON.parse 成功；顶层首字符为 [ （数组保持，写回用 JSON.stringify(j, null, 2) 且 j 为原始 parse 结果不拆包）。
2. 残扫：替换后全文不再含「接下来的场景中是否会有黄毛出现的可能」「后续剧情是否有黄毛实际出场的契机」「prologue 不展开该场景外戏」等 OLD 关键短语（NEW 中不含这些短语）。
3. 每处 NEW 存在。

---

# R8 扩展：NTRS 12 版（原 Cirno_NTRS_turn_edit_*_4.7.json，十二版）—— 黄毛刷新/行动机制同改

> 用户 2026-08-06 决策：「黄毛刷新这一块 18 版应该一起改」。本扩展覆盖原 NTRS 12 版（`Cirno_NTRS_turn_edit_{straight|FT|DEI}[_revise][_ALLin]_4.7.json`）。
> **关键语义（用户补充）**：NTRS 推进的行动判定（act/no-act）是「{{user}} 与对象**锁定后**」的判断——**锁定前（背景板/未锁定黄毛）可以 spawn 但一律 no-act**（黄毛积极行动门）。因此：
> - R8-A（spawn 基准收紧）适用：spawn 收紧到「{{user}} 当前场景画面内」——锁定前黄毛 spawn 也必须是在当前场景画面内才 spawn，同楼其他房间 → no_spawn
> - R8-B（场景外戏正文呈现）适用：已锁定黄毛场景外 act 时正文呈现
> - R8-C（同场主动制造机会 act）**仅对已锁定黄毛**生效——锁定前黄毛仍 spawn 但 no-act，不因 R8-C 改变「未锁定=no-act」的门
> - 原 NTRS 12 版无「目标离场时黄毛仍可 act」句、无「近身契机」BATTLE 式列举（其硬约束是「逻辑无硬伤门」散文式 + 「{{user}}-目标亲密互动避让门」）——C1/C2 的 BATTLE OLD 不适用，见下方 NTRS 专属 C1N/C2N

## N-A. spawn 基准收紧（NTRS 12 版 S2-MSG4）

### N-A1. 分支B spawn ①「刷新成功判定标准」句（十二版同，与 BATTLE A2 OLD 相同）
- OLD：`**刷新成功判定标准 = 接下来的场景中是否会有黄毛出现的可能**（后续剧情是否有黄毛实际出场的契机/进入画面的路径/与目标互动的机会）——**若黄毛仅是"存在"（如同楼住户/远房路人）但当前及后续场景都没有其出场与互动的可能 → 判 no_spawn，不空刷新**`
- NEW（同 BATTLE A2）：`**刷新成功判定标准 = 本轮黄毛能否进入 {{user}} 当前场景画面**（本轮当场出现 / 本轮内有合理进入画面的路径）——**黄毛不在 {{user}} 当前场景画面内（同楼其他房间/隔壁/附近/远房等，即使后续可能有出场机会）→ 判 no_spawn，不空刷新**`

### N-A2. HARD RULES 3「出场可能性判定」整段（十二版同，与 BATTLE A1 OLD 相同，注意 revise 版缩进 3 空格）
- OLD：`3. **出场可能性判定（刷新成功标准，替代纯时空合理性）**：\n - 必须从角色卡、世界书、背景设定、当前剧情线、后续场景趋势中查证：**接下来的场景中该黄毛是否有实际出现的可能**（有出场契机/进入画面的路径/与目标互动的机会）？\n - 若黄毛只是"存在"但当前与后续场景都没有出场与互动的可能（如同楼住户、远房路人，{{user}} 与对象在家私密互动时黄毛在自己家毫无关联）→ 判 **no_spawn**（不空刷新）\n - 若接下来的场景中黄毛有合理出场路径（目标将去公共场所、黄毛可被引荐/偶遇/主动接近、后续互动有展开空间）→ 判 **spawn**`
- NEW（同 BATTLE A1）：`3. **出场可能性判定（刷新成功标准，替代纯时空合理性）**：以 **{{user}} 本轮当前场景画面** 为唯一基准——黄毛**本轮能否进入 {{user}} 当前场景画面**（本轮当场出现 / 本轮内有合理进入画面的路径与目标互动）→ 判 **spawn**；**黄毛不在 {{user}} 当前场景画面内**（同楼其他房间、隔壁、附近区域、远房等——即使后续轮次可能有出场机会）→ 一律判 **no_spawn**（不空刷新）。黄毛与配对对象在本轮 {{user}} 场景之外互动 = 场景外行动（no_spawn 仍可判 act，见行动判定段），与 spawn 判定无关。`
  - 注意 revise 版该段行首为 3 空格（`   - 必须从…`），plain/ALLin 版为 1 空格（` - 必须从…`）——按各版 dump 精确取 OLD。

### N-A3. no_spawn 头注（十二版）
- OLD（plain/revise 版）：`- **no_spawn**：本轮无黄毛在场。两种情形：`
- OLD（ALLin/revise_ALLin 版）：`- **no_spawn**：本轮无黄毛在场。两种情形：`（同，但 ALLin 版子项缩进 1 空格）
- NEW（十二版）：`- **no_spawn**：本轮无黄毛在 {{user}} 当前场景画面内（同楼其他房间/隔壁/离场追踪/场景外行动——追踪中/离场黄毛仍可能行动）。两种情形：`

### N-A4. no_spawn ② 分支A 行（plain/revise 版）
- OLD（plain/revise，straight/FT/DEI 同）：`② 分支A——黄毛表已命中该目标黄毛但本轮在场不合理（如目标不在场、黄毛人设/场景与本轮冲突、黄毛表该行 lock_status=已离场 等），输出 no_spawn；若无历史锁定的活跃黄毛则下游 stage3 走快速通道。`
- NEW（plain/revise 十二版同）：`② 分支A——黄毛表已命中该目标黄毛但黄毛不在 {{user}} 当前场景画面内（如目标不在场、黄毛人设/场景与本轮冲突、黄毛表该行 lock_status=已离场、同楼其他房间等），输出 no_spawn——但若该黄毛已真正锁定且离场攻略目标（尾随/赶赴/在 {{user}} 场景外接近目标）在剧情上合理，仍可判 act（场景外行动，发生在 {{user}} 场景外）；行动判定为 no-act 时下游 stage3 走快速通道。`

### N-A5. no_spawn ② 分支A 行（ALLin/revise_ALLin 版）
- OLD（ALLin/revise_ALLin，straight/FT/DEI 同）：`② 分支A——黄毛表已有黄毛但本轮在场不合理（如黄毛人设/场景与本轮冲突、黄毛表该行 lock_status=已离场 等），输出 no_spawn；若无历史锁定的活跃黄毛则下游 stage3 走快速通道。`
- NEW（ALLin/revise_ALLin 十二版同）：`② 分支A——黄毛表已有黄毛但黄毛不在 {{user}} 当前场景画面内（如黄毛人设/场景与本轮冲突、黄毛表该行 lock_status=已离场、同楼其他房间等），输出 no_spawn——但若该黄毛已真正锁定且离场攻略目标（尾随/赶赴/在 {{user}} 场景外接近目标）在剧情上合理，仍可判 act（场景外行动，发生在 {{user}} 场景外）；行动判定为 no-act 时下游 stage3 走快速通道。`

### N-A6.（可选）分支A spawn ② 行补注（plain/revise 版）
- OLD：`② 分支A——黄毛表已命中该目标黄毛，本轮判定其在场合理，沿用已有黄毛`
- NEW：`② 分支A——黄毛表已命中该目标黄毛，本轮判定其在 {{user}} 当前场景画面内合理，沿用已有黄毛`
- （ALLin/revise_ALLin 版对应：`② 分支A（复用已有黄毛）——黄毛表已有黄毛条目，本轮判定其在场合理，沿用已有黄毛` → `② 分支A（复用已有黄毛）——黄毛表已有黄毛条目，本轮判定其在 {{user}} 当前场景画面内合理，沿用已有黄毛`）

## N-B. 场景外戏正文呈现（NTRS 12 版 S3）

### N-B1. S3-MSG0 act 编排「场景外」句（十二版，无 R7 补注）
- OLD（plain/revise，straight/FT/DEI 同）：`**若黄毛与对象均在 {{user}} 当前场景之外、但两者可接触（黄毛离场前往对象所在处攻略），本轮黄毛行动发生在 {{user}} 场景外——stage 须标注「场景外场景」，prologue 不展开该场景外戏**`
- OLD（ALLin/revise_ALLin 版，措辞「该目标所在处」）：`**若黄毛与该目标均在 {{user}} 当前场景之外、但两者可接触（黄毛离场前往该目标所在处攻略），本轮黄毛行动发生在 {{user}} 场景外——stage 须标注「场景外场景」，prologue 不展开该场景外戏**`
- NEW（十二版统一）：`**若黄毛与对象均在 {{user}} 当前场景之外、但两者可接触（黄毛离场前往对象所在处攻略，或黄毛制造对象离场契机后场景外接触），本轮黄毛行动发生在 {{user}} 场景外——stage 须标注「场景外场景」，正文 content 完整编排该场景外戏（读者可见黄毛与对象的互动全貌，{{user}} 角色不知情，属 📹 事后知情或 🌙 完全不知的暗线戏），prologue 不把该戏作为 {{user}} 在场戏展开**`
  - ALLin/revise_ALLin 版 NEW 中「黄毛与对象」→「黄毛与该目标」，「对象所在处」→「该目标所在处」，「对象离场契机」→「该目标离场契机」（保持 OLD 的「该目标」措辞，NEW 其余语义相同）。

### N-B2. S3 stage 模板「场景外标注」字段（十二版，无 R7 补注）
- OLD（十二版同）：`- **场景外标注:** 仅当本轮黄毛与对象均在 {{user}} 当前场景之外、黄毛离场前往对象所在处攻略时填「场景外场景」——该戏发生在 {{user}} 视线外，{{user}} 不知情，stage 记录、prologue 不展开`
- NEW（十二版）：`- **场景外标注:** 仅当本轮黄毛与对象均在 {{user}} 当前场景之外、黄毛离场前往对象所在处攻略（或制造对象离场契机后场景外接触）时填「场景外场景」——该戏发生在 {{user}} 视线外，{{user}} 不知情，**stage 记录 + 正文 content 完整编排该场景外戏（读者可见全貌）**`
  - ALLin/revise_ALLin 版若该字段用「该目标」措辞，保持「该目标」替换。

### N-B3. S3-MSG2 prologue 黄毛登场门 no_spawn 行（十二版，措辞与 BATTLE 不同）
- OLD（plain/revise/ALLin/revise_ALLin 各版措辞微异，按 dump 精确）：
  - plain/revise：` - thugSpawn 状态=no_spawn → 本轮无新黄毛登场；prologue 登场名单不列入新黄毛；若有上一轮已锁定的活跃黄毛则仍按"真正锁定"规则登场。`
  - ALLin/revise_ALLin：按 dump 实际（可能有「该黄毛」措辞）。
- NEW（十二版）：` - thugSpawn 状态=no_spawn → 本轮无新黄毛登场；prologue 登场名单不列入新黄毛；若有上一轮已真正锁定的活跃黄毛，且其本轮 act 行动发生在 {{user}} 当前场景内，则按"真正锁定"规则登场编排；若该黄毛本轮 act 为场景外行动（黄毛与对象均在 {{user}} 当前场景之外），该戏写入 stage（标注「场景外场景」）+ 正文 content 编排（读者可见，{{user}} 角色不知情），prologue 不展开、黄毛不列入登场名单。`

## N-C. 同场主动制造机会 act（NTRS 12 版，仅已锁定黄毛适用）

### N-C1. S2-MSG0「黄毛行动不依赖本轮是否刷新在场」句尾补「同场制造离场契机」（十二版）
- OLD（plain 版）：`**黄毛行动不依赖本轮是否刷新在场**——只要黄毛离场攻略目标（尾随/赶赴/在 {{user}} 场景外接近目标）在剧情上合理，即使本轮 no_spawn、目标与黄毛均不在 {{user}} 当前场景，也可判 act（该行动发生在 {{user}} 场景外）。`
- OLD（revise/ALLin/revise_ALLin 版：该句嵌在「判断该黄毛本轮是否可行动（合理→spawn，不合理→no_spawn；**黄毛行动不依赖本轮是否刷新在场**——…也可判 act，该行动发生在 {{user}} 场景外）」内，前后有括号；ALLin 版「该黄毛」措辞）——按 dump 精确取整句。
- NEW（十二版，plain 版）：`**黄毛行动不依赖本轮是否刷新在场**——只要黄毛离场攻略目标（尾随/赶赴/在 {{user}} 场景外接近目标）在剧情上合理，即使本轮 no_spawn、目标与黄毛均不在 {{user}} 当前场景，也可判 act（该行动发生在 {{user}} 场景外）。**目标与 {{user}} 同处当前场景时（黄毛已真正锁定）**：黄毛虽不在 {{user}} 当前场景画面内（no_spawn），但可主动制造目标离开 {{user}} 场景的机会（约定/诱引/传递信息/外部事件引走目标等合理手段）后，在 {{user}} 场景外对目标展开行动——场景外行动照常判 act。**未锁定（背景板）黄毛仍一律 no-act，不适用本条**。`
  - revise/ALLin/revise_ALLin 版按各自 OLD 结构补同语义句（保持「该黄毛/该目标」措辞一致）。

### N-C2.（可选）S2-MSG4 行动判定 no-act 定义句「未真正锁定…天然 no-act」补注
- OLD：`- **no-act**：本轮黄毛不出手。可能是：未真正锁定（背景板/未锁定黄毛天然 no-act）、或已锁定但本轮该留白等待时空/人设/动机成熟、或在两条硬约束下当前不可出手。no-act 时下游 stage3 走快速通道（跳过导演分析，prologue 仅一行主线推进）。`
- NEW：`- **no-act**：本轮黄毛不出手。可能是：未真正锁定（背景板/未锁定黄毛天然 no-act，锁定前可 spawn 但不得 act）、或已锁定但本轮该留白等待时空/人设/动机成熟、或在两条硬约束下当前不可出手（含目标与 {{user}} 同场且黄毛无合理制造离场契机的手段）。no-act 时下游 stage3 走快速通道（跳过导演分析，prologue 仅一行主线推进）。`

## N 版执行注意
- 12 个文件（3 类型 × plain/revise/ALLin/revise_ALLin 4 变体）。变体差异：revise 版缩进 3 空格、S2 任务名「黄毛判定·输入校准」含 userCalib 三步；ALLin 版 1:N 黄毛模型（全场唯一黄毛）、「一黄毛通吃」、分支A/B 措辞为「黄毛表已有黄毛条目/黄毛表无任何黄毛条目（首场首次）」；revise_ALLin 版两者叠加。
- 按各版 dump 精确取 OLD；NEW 语义统一，措辞随版本文体（该黄毛/该目标/黄毛）调整。
- 验证同 R8 主规格：JSON 合法、顶层数组、残扫 OLD 短语 0、NEW 存在。
